# apps/api-server

Placeholder for the future API/server application package.

Planned destination for:
- current server entrypoints in `src/index*.mjs`
- route assembly from `src/routes/`
- API bootstrap/config wiring

Current status:
- no server code has been moved yet
- existing runtime continues to live under `src/`
