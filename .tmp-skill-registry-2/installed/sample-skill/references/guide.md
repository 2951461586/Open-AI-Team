# Sample Skill Guide

This folder mirrors the OpenClaw skill shape:

- `SKILL.md` for human-readable activation rules
- `tools/` for runtime extensions
- `references/` for bundled docs
- `skill.json` for machine-readable manifest metadata

The runtime adds a small prompt appendix and one synthetic tool.
