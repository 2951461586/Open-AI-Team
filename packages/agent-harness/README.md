# @ai-team-harness/agent-harness

Planned home for the agent execution substrate: harness runtime, worker lifecycle, sandbox integration, plugin/tool execution, and host bridges.

Expected responsibilities:
- agent shell and product shell runtime
- sandbox and process/runtime adapters
- plugin and tool runtime support
- provider/host bridge abstractions

Migration note:
- primary source candidate is `src/agent-harness-core/`
