# 2026-03-11

## OpenClaw compaction / memory-flush 排障结论

- 在 `violet` 上，真实 QQ 原生命令入口 `/compact` **是通的**；不能再把问题归类为“slash 命令入口失效”。
- 主会话 transcript `/root/.openclaw/agents/violet/sessions/1c6fa388-1b7d-4b7c-b69a-c83800797cec.jsonl` 中已出现真实 `type: "compaction"` 记录：
  - `timestamp: 2026-03-11T05:03:03.223Z`
  - `tokensBefore: 123466`
  - `fromHook: true`
- 但 `sessions.json` 同一时间仍显示：
  - `compactionCount = 0`
  - `memoryFlushAt = null`
  - `memoryFlushCompactionCount = null`
  说明 **transcript 已压缩，但 session store 统计/回写链存在错位或缺陷**。
- 重要纠偏：`sessions.json.contextTokens = 200000` 在 OpenClaw 当前实现里，很多时候代表**模型 context window / capacity fallback**，不是“当前实际已使用上下文 token”。
  - 不能再用这个字段单独判断“会话是否已经顶满、是否发生 compaction”。
  - 后续判断 compaction 成败，应优先看：transcript `compaction` entries、`tokensBefore`、run meta、verbose/runtime 日志。
- 当前更该追的源码链路：
  - `persistRunSessionUsage(...)`
  - `incrementCompactionCount(...)`
  - `compactionCount` / `memoryFlushAt` / `memoryFlushCompactionCount` 写回 `sessions.json` 的路径
- `legacy` A/B 隔离已经在 `violet` 上落地过：`plugins.slots.contextEngine = "legacy"`，Gateway 曾在 `2026-03-11 12:34:00 CST` 重启生效。后续排查不能再简单假设“切 legacy 后仍无 compaction”；要以 transcript 实际 compaction 记录为准。
- 当前主会话 transcript 已非常脏，`toolResult` 体积异常大，是 compaction 性能风险源之一：
  - 文件约 `3.07 MB`
  - `toolResult` 文本累计约 `1,162,181` chars
  - 至少两个单条 `toolResult` 约 `200k` chars 级别
- OpenClaw 2026.3.8 的 embedded Pi compaction 安全超时常量是：
  - `EMBEDDED_COMPACTION_TIMEOUT_MS = 300000`（5 分钟）
  后续若再看到 `Compaction timed out`，要结合 transcript 巨型 `toolResult` / summarization 负载来判断，而不是先怪 context-engine。

## Built-in compaction 根修尝试（2026-03-11 13:24 CST）

- 已对内置 bundled 运行时文件打最小热补丁：
  - 文件：`/root/.nvm/versions/node/v22.22.0/lib/node_modules/openclaw/dist/plugin-sdk/dispatch-DwgTiP0N.js`
  - 备份：`dispatch-DwgTiP0N.js.bak-20260311-1323`
- 这次补丁**不改** `openclaw.json`、**不改**本地 `memory-lancedb-context` / `memory-lancedb-pro` 插件，只改 built-in `legacy` compaction 主路径。
- 新增了 compaction 前的 `toolResult` 规范化逻辑：
  - `getMessageText(...)`
  - `isNoisyToolResultText(...)`
  - `summarizeToolResultForCompaction(...)`
  - `normalizeMessagesForCompaction(messages)`
- 策略要点：
  - 对超大或明显 noisy 的 `toolResult`（如 SVG / XML / HTML / docs 大块）不再原样喂给 compactor。
  - 改为仅保留 `toolName`、字符数、行数、错误标记、head/tail 摘要。
  - 初始阈值：单条 `toolResult` 超过约 `6000` chars 或总预算超过约 `40000` chars 时触发摘要化。
- 在 `compactEmbeddedPiSessionDirect(...)` 主链里接入了该预处理，并新增日志锚点：
  - `toolResult normalization applied`
  - `normalization delta ... toolResultCharsBefore / After`
- 补丁后已执行：
  - `node --check` 语法校验通过
  - 清理 `/tmp/jiti/*`
  - `systemctl --user restart openclaw-gateway`
- Gateway 重启成功：
  - `MainPID = 607440`
  - `ActiveEnterTimestamp = Wed 2026-03-11 13:24:35 CST`
- 当前状态结论：
  - **补丁已部署并生效**
  - 但在记录该条记忆时，**尚未完成新一轮 `/compact` 端到端复测**，因此还不能宣称 timeout 已彻底修复。
- 一个启动后观察到的非阻断告警：
  - `[openclaw] missing tool result in session history; inserted synthetic error result for transcript repair.`
  这更像 transcript 修复提示，不是 Gateway 启动失败。
- 端到端复测随后拿到成功回执：
  - `Compacted (116k → 32k) • Context 32k/200k (16%)`
- 这说明本次 built-in `legacy` compaction 热补丁至少在当前 `violet` 主会话场景下**已实证修复 manual /compact 超时问题**。
- 当前可以成立的新判断：
  - 之前的 `Compaction timed out`，主因确实是 compactor 直接吞食超大原始 `toolResult`。
  - 在 compaction 前先做 `toolResult` 摘要化 / 预算裁剪，能够让同一会话成功从约 `116k` 压到约 `32k`。
- 后续若要正式化，应考虑把这次热补丁沉淀为源码级修复，而不是长期依赖直接改 bundled `dist` 文件。
- 架构收口已完成：本机先收口到 `memory-lancedb-pro` 作为 memory slot、`legacy` 作为阶段性 contextEngine，`memory-lancedb-context` 已通过安全链路禁用并重启验证成功。

## Lossless-claw 切换与验收（2026-03-11 晚间）

- 当晚继续推进 `lossless-claw`，最终已在 Violet 上完成切槽：
  - `plugins.slots.contextEngine = "lossless-claw"`
  - `plugins.entries["lossless-claw"].enabled = true`
  - `plugins.allow` 已包含 `lossless-claw`
- 初次切换失败的直接根因已确认：
  - `lossless-claw` 插件目录虽然存在，但**未进入 `plugins.allow`**，因此 `openclaw plugins info` 一度显示：
    - `Status: disabled`
    - `Error: not in allowlist`
  - 同时 `~/.openclaw/extensions` 中残留 `.openclaw-install-stage-*` 目录，造成 duplicate plugin id 警告。
- 正确修复链路已实证：
  1. 清理 stage 残留目录
  2. 将 `lossless-claw` 加入 `plugins.allow`
  3. 在 `contextEngine = legacy` 下先验证插件变为 `Status: loaded`
  4. 再切 `plugins.slots.contextEngine = "lossless-claw"`
- 切换后当前运行状态：
  - `openclaw plugins info lossless-claw` 返回 `Status: loaded`
  - Gateway 正常运行
  - 当前 QQ 私聊主会话仍可持续工作
- 验收结果：
  - `lcm_grep`：通过
  - `lcm_describe`：通过
  - `lcm_expand_query(summaryIds=...)`：通过
  - `lcm_expand_query(query=...)`：通过，但对 query 词更敏感；空命中不等于 expand 子链故障
- 当前兼容性提示：
  - OpenClaw 2026.3.8 下 `lossless-claw` 会提示：
    - `runtime.modelAuth is unavailable; using legacy auth-profiles fallback`
  - 这不是阻断错误，但说明当前宿主版本不是该插件的最优组合。
- 当前可成立的保守结论：
  - Violet 已基本完成从 `legacy` 到 `lossless-claw` 的切换
  - 核心 LCM 能力已能工作
  - 现阶段适合进入试运行观察。

## laoda：lossless-claw 切槽成功与真验证（2026-03-11 深夜）

- laoda 当晚也已完成从 `legacy` 到 `lossless-claw` 的切槽：
  - `plugins.slots.contextEngine = "lossless-claw"`
  - `plugins.entries["lossless-claw"].enabled = true`
  - `plugins.allow` 已包含 `lossless-claw`
- 首次切槽失败并回滚后，已做只读排锁，结论偏向：**不是持久坏锁，而是首次建库阶段的瞬时并发抢库**。
  - 当时 `~/.openclaw/lcm.db` 一度是 `0B` 空文件
  - 随后 `lsof/fuser` 未发现持续占锁者
  - `lossless-claw` 源码里同一路径 DB 在**同一进程内**有连接复用缓存，因而更像**跨进程并发**而非插件进程内自锁
- 第二次采用“干净重试”后成功：
  1. 仅通过 `openclaw-config-safe-edit` 改配置并重启 gateway
  2. 启动后**只看 `journalctl`**，不再立刻跑 `openclaw plugins info` / 其他会二次摸库的 CLI 探测
  3. journal 明确出现：
     - `[lcm] Plugin loaded (enabled=true, db=/root/.openclaw/lcm.db, threshold=0.75)`
     - 未再出现 `database is locked`
- 随后做了“真验证”（不是只看加载日志）：
  - 通过本地网关调用：
    - `openclaw agent --session-id lcm-verify-20260311 --message "Reply with exactly OK_LCM_VERIFY" --thinking off --json`
  - 返回成功，输出 `OK_LCM_VERIFY`
- 验证后 `~/.openclaw/lcm.db` 已从空库变为真实落表：
  - 表存在：`conversations` / `messages` / `message_parts` / `summaries` / `summary_messages` / `summary_parents` / `large_files` / `context_items` 及 FTS 相关表
  - 当前计数：
    - `conversations = 1`
    - `messages = 2`
    - `message_parts = 2`
    - `summaries = 0`
- 这说明 laoda 当前不只是“插件加载成功”，而是：
  - **LCM 首次迁移已落地**
  - **ingest/bootstrap 主路径已实际跑通**
  - **已具备进入试运行观察的条件**
