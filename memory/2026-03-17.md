# 2026-03-17

- 【heartbeat补记】00:10：新的一天开始；暂无新增事项。延续“低噪音、连续性优先”的 heartbeat 策略。
- 【heartbeat补记】08:37-08:40：实测 edgefn (`https://api.edgefn.net/v1`) OpenAI 兼容接口：`GET /models` 200 且包含 `MiniMax-M2.5`、`GLM-5`；`MiniMax-M2.5` 可正常 chat 但输出常带 `<think>...</think>`（需客户端过滤）；`GLM-5` 出现 `reasoning_content` 与 `content` 分离且频繁 503(ChannelNotEnough)/429(RateLimitExceeded)，可用性波动。
- 【heartbeat补记】12:33：检查本机 OpenClaw `qqbot` 插件：已加载，版本 `1.5.4`（@sliverp/qqbot@latest，安装于 2026-03-13）。`openclaw doctor` 同时提示：`groupPolicy=allowlist` 但 allowlist 为空会导致群消息静默丢弃。
- 【heartbeat补记】12:39：dry-run 检查插件更新：`openclaw plugins update qqbot --dry-run` 显示可从 `1.5.4 -> 1.6.1`；同时有安全扫描 WARNING（env+network、child_process 等），属“能力风险提示”。
