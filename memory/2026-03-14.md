# 2026-03-14

- 继续推进 qqbot richer-message / qqbot-config 项目时，明确了当前产品层默认分流：**日常聊天、项目推进、需要用户做选择的场景优先走 `markdown + keyboard`；新闻汇总、GitHub 热榜、状态播报、摘要通知这类展示型内容优先走 `ark37`，单行短摘要可用 `ark24`。**
- 已将这条策略写入 `skills/qqbot-config/SKILL.md` 与 `skills/qqbot-config/references/openclaw-mapping.md`，避免后续插件实现和高层接入跑偏。
- 当前共识不是“全量卡片化”，而是**交互层与展示层分开设计**：交互优先稳定按钮链路，展示优先利用 Ark 卡片承载摘要。
- 已继续往前实现高层 preset：在 `~/.openclaw/extensions/qqbot/src/templates.ts` 新增 `interactive-choice` / `digest-card` / `short-bulletin` 三类场景化模板构建器，并在 `src/channel.ts` 接通 `card.qqbot` / `channelData.qqbot` 下的 `preset` 识别；`./node_modules/.bin/tsc --noEmit` 已通过。
- 已完成 A 路线：新增 `scripts/qqbot-preset-smoke.mjs` 与 `scripts/qqbot-preset-regression.mjs`，并在 `package.json` 注册 `npm run smoke:preset` / `npm run smoke:preset-regression`；对新回归脚本执行 `--dry-run` 已通过，可用于一键真机验证 3 个 preset 场景。
- 已继续完成 B 的第一段：`message action=send` 的高层入口新增“通用参数 → preset”自动映射，保持显式 `card.qqbot / channelData.qqbot` 优先级不变；其中 `buttons / card.buttons` 自动落到 `interactive-choice`，通用 `card(title + summary)` 自动分流到 `digest-card` 或 `short-bulletin`。另新增 `npm run smoke:highlevel-send` 做高层通用参数 dry-run 验证，并已对 `buttons / digest / bulletin` 三种 case 跑通。
- 已完成 B-2：补齐真机回归配套，新增 `scripts/qqbot-highlevel-send-observation-md.mjs`、`scripts/qqbot-highlevel-send-regression.mjs`，并注册 `npm run highlevel-send:report-md` / `npm run smoke:highlevel-send-regression`；构建后对新 regression runner 执行 `--dry-run` 已通过，可用于一键验证高层 generic send 在 QQ 客户端上的真实表现。
- 已完成 B-3：收紧 generic `card(title + summary)` 的 digest / bulletin 分流策略。新增 `meta.routeHint` 显式提示支持（如 `digest` / `bulletin` / `ark37` / `ark24`），并把原先粗阈值升级为更保守的打分式判定：只有摘要信号足够强才走 `digest-card`，否则默认落 `short-bulletin`；同时已用 `qqbot-highlevel-send-smoke.mjs` 对 hint 覆盖场景做 dry-run 验证通过。
- 已完成 B-4：新增产品向接入文档 `docs/qqbot-generic-send-integration-guide.md`，系统化固化 generic send 的推荐 payload、`routeHint` 使用规则、适用/不适用场景、决策树与回归命令；并已挂到 `README.md` / `README.zh.md` / `docs/qqbot-output-selection-strategy.md` / `qqbot-config` 技能映射文档入口。
- 已推进“方向 2”收尾：新增 `docs/qqbot-richer-message-generic-send-rollout.md`，把本轮 richer-message / generic send 改造整理成可直接复用的 PR / 提交 / 发布说明口径，并补挂到 README 与选型策略文档入口。
- 已开始 C-1：把 QQ 私聊里的“选项型普通回复 → markdown + keyboard” 自动化接入普通 reply 链路。当前实现只在 `replyToId` 存在、目标为 QQ 私聊、文本明显是 A/B/C 或 1/2/3 选项型回复、且没有显式 structured payload 时触发；新增 `buildAutoStructuredReplyMessage(...)` 与 `npm run smoke:reply-choice`，并已验证 `ab` / `123` 会转 keyboard、`negative` 不会误触发。
- 对当前 `violet` 账号在真实 QQ 私聊被动回复路径做了 C-1 真机打样：选项型 reply 确实进入了 keyboard 出站逻辑，但 QQ API 直接返回 `not allowd custom keyborad`；同路径普通纯文本对照组发送成功。结论：当前问题不是路由没生效，而是 **当前 app / 权限 / 路径 不允许 custom keyboard**，因此文档中关于 keyboard 的口径已改为“环境相关，需按当前账号/路径单独验”。
- 已补代码收口：`sendStructured(...)` 现在遇到 QQ API 的 keyboard 拒绝错误时，会自动去掉 keyboard、回退成纯 markdown 再重发；真机验证已通过，当前账号下“选项型回复”不再整条失败，而会保底投递成普通文本。`openclaw.json` 当前本地配置已经有 `channels.qqbot.capabilities.inlineButtons=all` 和 `markdownSupport=true`，说明这次不是本地开关没开，而是 QQ 侧实际权限/路径限制。
- 继续排查差异后确认：把 C-1 自动按钮的 `callback_data` 从中文整句改成 ASCII 安全格式（`auto_choice:alpha:1`）后，**当前 QQ 私聊被动回复路径仍然报同样的 `not allowd custom keyborad`**。因此当前更可信的结论是：不是 callback_data 内容问题，而是 **proactive/explicit send 路径 与 passive reply 路径** 在 QQ 侧的 keyboard 接受策略不一致。
- 已完成最关键的对照矩阵：使用**同一份 markdown+keyboard payload** 直接调用 QQ API，`C2C proactive send` 成功送达，而 `C2C passive reply`（带 `msg_id`）稳定报 `not allowd custom keyborad`。结论正式收口：**当前 `violet` 账号下，keyboard 不是全局不可用；它是“主动可用、被动回复不可用（至少当前路径如此）”。**
- 用户随后点击了主动消息矩阵按钮并回流 `matrix:ok`，进一步确认：**当前账号下 proactive keyboard 不仅能发出，而且按钮点击后的文本 callback 回流也正常。**
- 又补了一轮“官方文档 + 最小代码对照”核验：QQ 官方发送消息文档里，单聊接口明确把 `keyboard object`、`msg_id`、`msg_seq` 同时列为合法请求参数，**文档并未写“被动回复禁止 keyboard”**；但同一 endpoint 下，`passive markdown-only` 成功、`passive markdown+keyboard` 失败、`proactive markdown+keyboard` 成功。结论进一步偏向：**不是本地代码组包错误，而是 QQ 服务端对 `C2C passive reply + keyboard` 组合存在未文档化的限制/权限门槛/平台侧行为差异。**
- 当前阶段性结论先保守收口为：**在已验证的 `violet` / QQ C2C 对话路径下，不应把 keyboard 当作 passive reply 默认能力；keyboard 适合继续放在 proactive / explicit structured send 路径。** 但用户希望在正式封板前再补查两项残余变量：`event_id` 路径、`group passive reply + keyboard` 路径。
- 已完成 B-1：给 `qqbot` gateway / 调试链路补了 `event_id` 可观测性。当前做法是把 WebSocket envelope 的 `payload.id` 作为 `eventId` 带入 `QueuedMessage`、处理日志、agent 上下文（显示“事件ID”）以及 inbound payload（`QQEventId`）。这样下一条真实消息进来时，就能直接拿到 `event_id` 做被动 keyboard 复验，而不必再翻原始包。
- 已完成 `event_id` 真实复验：使用当前真实 C2C 入站的 `event_id` 直发被动 structured reply，测试了两组——`event_id + markdown + keyboard` 与 `event_id + markdown only`。两者都返回同一错误：`请求参数event_id对应事件不能回复消息`。结论：**当前 `C2C_MESSAGE_CREATE` 这条私聊入站事件不能拿 `event_id` 作为 reply token**；这不是 keyboard 特有问题，而是 `event_id` 在该事件类型上本身不可回复。结合此前 `msg_id + keyboard` 返回 `not allowd custom keyborad`，当前 `violet` / C2C 被动路径应收口为：`msg_id` 可做普通被动回复，但 keyboard 不应作为默认能力；`event_id` 不能作为这条路径的补救方案。
- 已完成 A（文档/技能收口）：同步更新了 `extensions/qqbot/README.zh.md`、`extensions/qqbot/README.md`、`extensions/qqbot/docs/qqbot-richer-message-status.md`、`skills/qqbot-config/SKILL.md`、`skills/qqbot-config/references/openclaw-mapping.md`。统一口径为：**C2C 被动 reply 默认走 markdown/text；keyboard 只承诺在主动或显式 structured send 路径；`event_id` 不作为当前 C2C 事件的 reply fallback。**
- 已推进 B（group 路径真实复验）并拿到 fresh `GROUP_AT_MESSAGE_CREATE` 样本：`group_openid=198CFF929FDC11BE9820CEC4664039F3`，`messageId=ROBOT1.0_gach1FUyVlR3MWFyR3rlVdKcB07L7lp06aoBT4of1KI0uTCIfi0RlO1dqDM5w0UdMWdZFyXhQnOIlz6ZZzNls9NE-gvAa3n5NBhUf3D0SlY!`，`eventId=GROUP_AT_MESSAGE_CREATE:gach1fuyvlr3mwfyr3rlvtcklif86mc7krcsdiviailxqkgxwpynhysl6q6ws849`。实测结果：`msg_id + markdown only` 与 `msg_id + markdown + keyboard` 都返回 `回复消息msg_id已过期`（无法用这轮样本区分 keyboard 与 baseline markdown）；`event_id + markdown only` 与 `event_id + markdown + keyboard` 都返回 `请求参数event_id对应事件不能回复消息`；`group proactive markdown + keyboard` 返回 `主动消息失败, 无权限`。当前可下结论：**`GROUP_AT_MESSAGE_CREATE` 的 `event_id` 也不能作为 reply fallback；当前 `violet` app 对 group proactive send 无权限；而 group passive `msg_id` 能否带 keyboard 仍需更快的一轮 fresh replay 才能最终收口。**
- 老板决定：**B 先不继续追最后一锤，按当前证据先收口**。后续如果业务上再次需要 group passive keyboard 结论，再专门做一轮“收到 fresh 群消息后 30 秒内双发（markdown only / markdown+keyboard）”的快测。
- 已完成 B 的最终文档收口：继续同步更新 `extensions/qqbot/docs/qqbot-richer-message-status.md`、`extensions/qqbot/README.zh.md`、`extensions/qqbot/README.md`、`skills/qqbot-config/SKILL.md`、`skills/qqbot-config/references/openclaw-mapping.md`。统一新增 group 结论：**`GROUP_AT_MESSAGE_CREATE` 的 `event_id` 也不能作为 reply fallback；当前 `violet` app 的 group proactive markdown+keyboard 无权限；group passive msg_id + keyboard 暂不写成已验证可用能力。**
- richer-message 主线继续推进：新增产品/业务视角文档 `extensions/qqbot/docs/qqbot-richer-message-product-playbook.md`，把主线策略正式收成“**聊天交互走 markdown+keyboard，展示播报走 Ark，普通被动 reply 默认走 markdown/text**”；并在 `README.zh.md`、`README.md`、`qqbot-output-selection-strategy.md`、`qqbot-generic-send-integration-guide.md`、`qqbot-richer-message-generic-send-rollout.md` 中补了入口链接，方便后续从产品、接入、回归三层读同一套口径。
- 继续做 A（examples / cookbook）：新增 `extensions/qqbot/docs/qqbot-richer-message-cookbook.md`，集中放可复制的高层 payload 示例（`buttons`、generic `card`、显式 `preset`、`card.qqbot`、`channelData.qqbot`、普通被动 reply 安全写法）；同时把 README、playbook、generic-send guide、rollout 文档都挂上 cookbook 入口，后续接入方可以优先抄示例而不是翻低层试验记录。
- 继续做 A-2（场景化 examples）：新增 `extensions/qqbot/docs/qqbot-richer-message-scenario-examples.md`，按真实业务场景组织 richer-message 接入示例（如日常聊天选项、CI 失败处理、GitHub 热榜、项目摘要、部署完成、异常告警“先播报再按钮”、普通助手回复、定时摘要推送）；并把 README / playbook / cookbook / rollout 文档继续补上场景化入口，形成“场景 -> 示例 -> 规则 -> 状态”的阅读路径。
- 继续做 A-3（examples -> smoke fixtures）：新增 fixture `extensions/qqbot/fixtures/qqbot-richer-message-scenarios.json`，把场景文档固化成可执行样例；新增脚本 `scripts/qqbot-scenario-smoke.mjs`、`scripts/qqbot-scenario-observation-md.mjs`、`scripts/qqbot-scenario-regression.mjs`，并在 `package.json` 挂出 `smoke:scenario` / `scenario:report-md` / `smoke:scenario-regression`。同时把 `qqbot-highlevel-send-smoke.mjs` 改为走真实高层 `actions.handleAction(send)` 路径，不再只测较偏底层的 `outbound.sendText`。已实跑：`npm run build && npm run smoke:scenario-regression -- --to qqbot:c2c:TEST_OPENID --account violet --dry-run`，通过。
- 继续做 A-4（README richer-message 总收口）：把 `extensions/qqbot/README.zh.md` 与 `extensions/qqbot/README.md` 中原本偏“状态堆叠 + 细项罗列”的 richer-message 大段，重构成更像产品入口页的结构：先给 TL;DR 默认产品路径，再明确 Stable / 环境相关 / Do-not-default 边界，再说明高层业务该怎么接（显式 QQ payload / generic send / preset），最后统一放文档阅读顺序与 smoke / regression 入口（scenario / auto-route / highlevel-send / experimental）。目标是让后续接入方一眼知道“先看啥、怎么接、怎么验”，减少在 README 里迷路。
- 新增三节点（`violet / laoda / lebang`）自动更新与 gateway 基线排查结论：`violet`、`lebang` 的 auto-updater 已具备 `PATH/NVM`、绝对路径 `npm/node/openclaw` 与 GitHub SSH→HTTPS fallback；`laoda` 当时缺少 updater 的 HTTPS fallback，且 `~/.config/systemd/user/openclaw-gateway.service.d/20-node-heap.conf` 强设 `NODE_OPTIONS=--max-old-space-size=2048`，日志里出现过多次 `Reached heap limit` OOM。
- 已在 `laoda` 远端完成修复并验证：备份目录为 `/root/.openclaw/backups/laoda-repair-20260314-200719`；移除了 `20-node-heap.conf`（去掉 2048 heap 限制），给 `scripts/auto-updater.sh` 的 OpenClaw 安装链路补上 GitHub HTTPS fallback，重启后 gateway `RPC probe: ok` 且继续监听 `127.0.0.1:18789`。
- ClawHub 故障定位有了明确结论：旧版 `clawhub` CLI `0.7.0` 下，`search / inspect / install` 会报 `Rate limit exceeded`，但 `explore` 可用；升级到 `0.8.0` 后，在当前代理环境下 `search weather`、`inspect github`、`install github` 均恢复正常。当前更可信的根因是 **旧版 CLI 与 registry / 限流处理兼容性问题**，而不是 OpenClaw 内置 skill/prompt 未更新。
- `laoda` 资源侧当前稳态：机器总内存约 `1.9 GiB`、swap `8 GiB`；检查时 swap 已用约 `1.0 GiB`。`openclaw-gateway` 为首要内存大户，RSS 约 `506 MiB`、`MemoryCurrent` 约 `781 MiB`、`MemoryPeak` 约 `1.25 GiB`、`MemorySwapCurrent` 约 `432 MiB`。结论：`laoda` 目前能跑，但整体内存偏紧，后续不宜继续无节制叠插件/长上下文/并发任务。
