# 2026-03-20

- 【heartbeat补记】00:25：新的一天开始；当前继续延续“低噪音、连续性优先”的节奏。主状态仍是按需处理老板的新请求，暂无需要主动外发的事项。
- 【heartbeat补记】01:20：当前有一条新的配置变更上下文需延续——laoda 侧正在把 OpenRouter 下 `hunter` 模型改为 `xiaomi/mimo-v2-pro`；已确认这才是 live 列表中的准确模型 ID，当前处于修正此前裸写 `mimo-v2-pro` 并继续验收的阶段。
- 【heartbeat补记】09:08：今早已对 `liliMozi/openhanako` 做一轮偏架构级深扫并与 OpenClaw 对比。当前判断可留档——OpenHanako 的优势主要在“个人桌面助理产品感更强、工程更轻、跨 session 记忆连续感更自然”；OpenClaw 的优势仍在“平台化能力上限更高、可扩展/可编排/可审计更强”。
- 【heartbeat补记】09:08：补一条真实系统行为——当前环境里的 `gh` CLI 未登录，直接 `gh repo view` 会失败；若只是做仓库结构分析，直接 `git clone` + 本地读源码比等 GitHub auth 更顺。
- 10:18～10:22：老板明确要求后续少讲文案、多动手执行；不走 MVP，直接沉淀一套面向 OpenClaw 的“记忆编译层”完整优化方案，并已在工作区落地第一批交付：`reports/openclaw-memory-compiler/MASTERPLAN.md`、`IMPLEMENTATION-BACKLOG.md`、schema 文件、`SOURCE-REFS.md`，以及 `memory/compiler/` 目录骨架与 `scripts/memory-compiler/` 基础脚手架（索引重建 / thread aging / source audit）。
- 10:24～10:26：继续按执行顺序直接落代码，已实现 `scripts/memory-compiler/lib/` 共享库、`fact-compiler.mjs`、`thread-compiler.mjs` 和示例输入；成功写入 `memory/compiler/facts.jsonl` 2 条、`threads.jsonl` 1 条，并跑通 `rebuild-indexes` 与 `source-audit`（无 issues）。
- 10:30～10:32：继续推进第二批代码，已实现 `continuity-compiler.mjs`、`digest-compiler.mjs`、`runtime-selector.mjs`，成功写入 `memory/compiler/continuity.jsonl`，生成 `memory/compiler/digests/today/2026-03-20.md`、`memory/compiler/digests/narrative/current.md` 与 digest manifests；runtime selector 已能按 `chat` 场景输出 facts / threads / continuity / digest snippets 组合块。
- 10:36～10:38：继续做大块能力，给 facts 增加 `subject/attribute/value` 结构字段，新增 `fact-conflicts.mjs` 冲突巡检、`pipeline-run.mjs` 全流水线 orchestrator，并跑通 bundle 一键入库：facts 现为 4 条、threads 2 条、continuity 2 条，week digest 已生成，fact conflict report 当前为 0 冲突，source audit 仍通过。
- 10:40～10:46：继续推进 ingest / lifecycle / adapter / governance 层，新增 `ingest-normalize.mjs`、`fact-lifecycle.mjs`、`adapters/lcm-summary-import.mjs`、`adapters/lancedb-memory-import.mjs`、`adapter-pipeline-run.mjs`、`compiler-status.mjs`、`fact-reconcile.mjs`、`thread-lifecycle.mjs`、`digest-gc.mjs`；已跑通 raw/LCM/lancedb 三路导入、precise/heartbeat/task selector、digest manifest 收敛与 latest-index 生成。随后补正一次 fact supersede：将旧自由文本规则 `fact_b25715176b76` 标为 stale，并合并入结构化 `fact_32792a0f99ef`。当前状态：facts 10（confirmed 8 / inferred 1 / stale 1）、threads 4（active 2 / closed 1 / blocked 1）、continuity 4、manifests 18。
- 10:47～10:54：继续推进治理与增量能力，新增 `thread-cluster.mjs`、`fact-dedupe.mjs`、`thread-cluster-apply.mjs`、`integrity-audit.mjs`、`fact-repair-ids.mjs`，并升级 `fact-conflicts.mjs`（支持 auto-dispute）、`digest-compiler.mjs`（支持 unchanged-content skip）、`runtime-selector.mjs`（支持 sourceRef-aware + qualityScore + coverage）、`pipeline-run.mjs`（输出 compilePlan，空 bundle 时不重编译 digests）。过程中发现并修复一个关键 bug：旧版 fact compiler 对同 subject/attribute 不同 value 提前合并，导致 conflict 检测失真；修正后又补了 duplicate-id repair 与 integrity audit，当前 integrity audit 已通过。当前状态：facts 13（confirmed 8 / inferred 1 / disputed 3 / stale 1）、threads 4、continuity 4、manifests 23。
- 10:58～11:02：继续推进并显式参考 OpenHanako 的方向约束，新增 `fact-arbitrate.mjs`，让 conflict/dispute 后有 source-first 仲裁流；同时升级 `thread-cluster.mjs` 为标题/摘要/source/relatedFacts 多因子聚类，升级 `runtime-selector.mjs` 支持 token budget，升级 `pipeline-run.mjs` 输出 `changedSourceRefsCount` 与来源前缀分布。过程中又发现一个关键偏航点：如果仲裁只看来源强度，会把“来源强但语义脏”的记录扶正，这与“源层负责真相”相冲突；因此进一步把 `fact-arbitrate.mjs` 改成 **source-first + 语义一致性校验**。demo conflict 经修正后已正确仲裁到 `fact_bb8cb991e3aa`，旧脏记录转 stale。当前状态：facts 13（confirmed 9 / inferred 1 / stale 3 / disputed 0）、threads 4、continuity 4、manifests 23。
- 11:04～11:06：继续往“源层纪律”方向推进，升级 `digest-compiler.mjs` 支持 `changedSourceRefs` 感知与 `no-source-impact` 跳过，升级 `pipeline-run.mjs` 支持 `autoArbitrateDisputes` 与 arbitration 后再跑 conflict report，并把 `changedSourceRefs` 传入 digest 编译；升级 `compiler-status.mjs` 输出 latest arbitration/integrity 摘要。用 continuity-only bundle 实测：compilePlan 已能区分 `today=true / week=false / narrative=true`；对 week digest 直接喂不相关 sourceRefs 时会返回 `reason: no-source-impact`。当前状态：facts 13、threads 4、continuity 5、manifests 25，integrity 仍通过。
- 11:08～11:11：继续把 source discipline 往运行时里压，升级 `runtime-selector.mjs` 支持 source coverage guard、blockCoverage、以及 digest 从 latest manifest 继承 sourceRefs；新增 `source-discipline-check.mjs`，并让 `compiler-status.mjs` 暴露 source discipline 健康摘要。巡检结果第一次明确暴露出存量问题：confirmed facts 里仍有 1 条 artifact-only、live continuity 里仍有 1 条 artifact-only；说明治理不再停留在原则，而是能把“派生层在偷偷带偏方向”的地方直接抓出来。当前状态：facts 13、threads 4、continuity 5、manifests 25；sourceDiscipline=false，integrity 仍通过。
- 11:13～11:15：继续往“自动纠偏”推进，新增 `source-discipline-enforce.mjs`，并把它接进 `pipeline-run.mjs`；现在 pipeline 在 `sourceDiscipline=false` 时可以自动执行治理，再次巡检并重建 indexes。实际执行结果：将 1 条 artifact-only confirmed fact（`fact_bb8cb991e3aa`）降级为 inferred，将 1 条 artifact-only live continuity（`continuity_b13989ee8fd8`）过期；治理后 source discipline 从 false 变为 true，confirmed facts / active threads / live continuity 三项 trustedRatio 全部恢复到 1.0。当前状态：facts 13（confirmed 8 / inferred 2 / stale 3）、threads 4、continuity 5、manifests 25；integrity 通过，sourceDiscipline 通过。
- 11:16～11:19：继续把 discipline gate 前移到 ingest/compiler 入口，新增共享库 `scripts/memory-compiler/lib/source-discipline.mjs`，并升级 `fact-compiler.mjs` / `thread-compiler.mjs` / `continuity-compiler.mjs`：artifact-only fact 若请求 `confirmed` 会在入库时直接降为 `inferred`，artifact-only active thread 会在入库时直接 `blocked`，artifact-only continuity 会在入库时直接带 `expiresAt` 过期。同时收紧 `runtime-selector.mjs`：task 场景下 digest 只要 manifest sourceRefs 混入 artifact，就直接从 prompt 候选中丢弃。用 `ingest-gate-demo` 实测：`fact_6a09185793b4` 被 ingest gate 降级、`thread_ab80cda1aa21` 被阻断、`continuity_d761f7e36b90` 被入库即过期；而 sourceDiscipline 仍保持 true。当前状态：facts 14（新增 1 条 gated inferred demo）、threads 5、continuity 6、manifests 28；运行时 task selector 已把 today digest 因 artifact 污染踢出 prompt 候选。
- 11:22～11:24：做整体初步验收前，又修了一个方向性问题：`digest-compiler.mjs` 之前仍可能把已过期 continuity / 非 trusted 记录重新编进 digest，等于让派生层绕过 gate。现已修正为 digest 只采纳 trusted confirmed facts / trusted active threads / 未过期且 trusted 的 continuity。随后删除验收用 `ingest-gate-demo` 脏记录，重编 today/week/narrative 并跑 `acceptance-smoke.mjs`。验收结果 **10/10 通过**：`source-discipline-ok`、today/week/narrative latest manifest clean、task selector no artifact digests、precise selector no digests/no continuity/source-first、pipeline discipline ok、pipeline integrity ok。当前状态回到：facts 13、threads 4、continuity 5、manifests 31；sourceDiscipline 通过，integrity 通过，latest manifests 已无 artifact 污染。
- 11:27～11:29：按老板要求回头对照 `MASTERPLAN.md`、`IMPLEMENTATION-BACKLOG.md`、当前 `compiler-status.mjs` 与 openhanako 对照结论，给出当前落地进度判断：**骨架层约 75% / 治理层约 65% / 运行时约 70% / 调度集成层约 35% / 长期产品化验收约 30%**。判断依据：数据模型/编译器/运行时 selector/治理审计/adapter/pipeline/初步验收已基本成形，但 session-end hook、daily/weekly/heartbeat/subagent 等系统接入、inferred→confirmed / disputed review 等人工治理流、长期回归和指标观测仍明显缺口。总体结论：已从方案期进入“可运行可初验”的中段，方向基本对齐 openhanako 与顶层设计里的源层优先、派生层节制。
- 11:32～11:34：继续补“最大缺口”——调度/集成层与人工复核流。新增 `trigger-plan.mjs`（从事件 bundle 推导 compile/review 计划）、`trigger-execute.mjs`（事件驱动执行入口）、`review-queue.mjs`（复核队列持久化），并升级 `compiler-status.mjs` 暴露 reviewQueue / latestTriggerPlan 摘要。实测用含 decision fact + inferred fact 的 bundle 跑通：自动触发 pipeline 编译 today/week/narrative，sourceDiscipline 仍为 true，同时把 1 条 inferred fact 自动写入 `memory/compiler/review-queue.jsonl` 形成 open review item。当前状态：facts 15、threads 5、continuity 6、manifests 34、reviewQueue open=1；说明系统已开始从“会编译”推进到“会按事件调度并为人工治理留入口”。
- 11:37～11:45：继续把“能长期跑”的骨架补实。新增 `review-apply.mjs`（review 决议回写 lifecycle + 重编 digests）、`scheduler-plan.mjs` / `scheduler-run.mjs`（统一调度 heartbeat / daily / weekly / session-end / subagent-complete），并新增 `memory/compiler/scheduler-state.json`、`memory/compiler/scheduler-history.jsonl` 记录执行状态和历史。`review-queue.mjs` 现在支持 status / reviewType / scope / factId / priority / query / limit 过滤查询；`acceptance-smoke.mjs` 也扩到调度覆盖。期间修掉一次 patch 失误：`scheduler-run.mjs` 的 `writeJson` 被错误换行导致 SyntaxError，修复后重跑 heartbeat/daily/weekly 均通过。最新状态：heartbeat=2 jobs、daily=6 jobs、weekly=6 jobs，acceptance smoke **13/13 通过**；counts 为 facts 17、threads 6、continuity 7、manifests 40，reviewQueue open=1/resolved=1，schedulerState runCount=7。整体已从“会调度”推进到“会调度、会留痕、会回写、会做基本周期维护”。
- 11:48～11:52：继续把系统往 openhanako 风格的“控制面”拧。`review-apply.mjs` 补成支持 **select + defaultDecision + dryRun** 的批处理/条件决议；新增 `hook-dispatch.mjs` 作为 hook → scheduler 的统一入口；`scheduler-run.mjs` 新增 `scheduler-audit.latest.json`，记录 plannedJobs / executedJobs / skipped / openReviews / lastRunAt / throttle 信息；`compiler-status.mjs` 也补了 `latestSchedulerAudit` / `latestHookDispatch` 暴露。实跑结果：batch review dry-run 命中 1 条 open review；hook-heartbeat 成功触发 scheduler-run；hook-session-end 成功触发 trigger-execute → pipeline，全链新增事实/线程/连续态各 1 条。最新 counts 为 facts 18、threads 7、continuity 8、manifests 43，schedulerState runCount=12；acceptance smoke 扩到 **16/16 通过**。这一步标志着系统开始具备“统一入口、统一调度、统一审计、统一复核”的雏形，而不是继续散着长脚本。
- 11:56～11:59：继续啃“重复触发 / 幂等 / 去抖”。`scheduler-plan.mjs` 增加 `eventFingerprint` 去重，且去重优先级高于 throttle；`hook-dispatch.mjs` 增加 `hookId` 幂等与 `memory/compiler/hook-events.jsonl` 事件账本；`scheduler-run.mjs` / `compiler-status.mjs` 暴露 `deduped`、`duplicateSkipped`、hook recent events 等状态。实跑结果：同 `hookId=manual-dup-hook` 第二次触发被 `duplicate-hook` 跳过；同 `eventFingerprint=manual-dup-fp` 的 scheduler 事件第二次被 `duplicate-event` 跳过。期间 acceptance smoke 因 dedupe 抢在 throttle 之前命中而失败 1 次，随即修正用例（显式关闭 dedupe 干扰）后重新跑通，现为 **18/18 通过**。当前控制面已从“统一入口”推进到“统一入口 + 幂等去重 + 事件账本 + 审计可见”。
- 12:04～12:06：继续把控制面往“不会白丢事件”的方向推。新增 `scheduler-enqueue.mjs`（跳过事件先入 `memory/compiler/scheduler-pending.jsonl`）与 `scheduler-drain.mjs`（统一 drain 执行 pending items），并让 `scheduler-run.mjs` 在 throttle / dedupe 命中时默认尝试 enqueue；`hook-dispatch.mjs` 也把 `enqueueOnSkip` / `coalesceKey` 往下透传；`compiler-status.mjs` 新增 `schedulerPending` 与 `latestSchedulerDrain` 面板。实跑结果：手工 enqueue 的两个 `session-end` pending item 均被 drain 成功，各自转成一次真正的 `session-end -> trigger-execute -> pipeline`；验收里又验证了一个 heartbeat pending item 能入队并被 drain。当前 counts 升到 facts 19、threads 8、continuity 8、manifests 47；`scheduler-pending.jsonl` 中已有 3 条 drained 账本；acceptance smoke 升到 **19/19 通过**。这一步让控制面开始具备“跳过不等于丢失，而是先积压、后归并、再统一出清”的味道，更像长期运行系统而不是脚本集合。
- 12:12～12:20：继续按“不做小修小补”的方向推进两大块。其一，把 pending control plane 再做实：`scheduler-enqueue.mjs` 新增 `priorityScore` / `coalesceWindow`，`scheduler-drain.mjs` 改成按 priority + aging + enqueueCount 计算 `effectiveScore` 再 drain；手工实测同 `coalesceKey=manual-coalesce-window` 的 heartbeat skip 事件已能真正合桶，随后又修掉一次 patch 引入的 `scheduler-drain.mjs` 语法错误（多余 `}`），重跑通过。其二，参考 openhanako 的 session/ticker 思路，但严格按顶层设计保持派生层节制，新增 `session-pack.mjs` 与 `memory/compiler/session-packs/current.json` / `history.jsonl`：把当前热态 facts/threads/continuity 组装成 **Session Continuity Pack**，带 `sourceRefs`、`expiresAt`、`sourceDiscipline`，并让 `runtime-selector.mjs` 新增 `scene=session` 优先消费该 pack；`pipeline-run.mjs` 也能在变更后自动重建 session pack，`compiler-status.mjs` 暴露 `latestSessionPack`。当前实测 pack 生成成功（7 个 sourceRefs、discipline ok），pending coalesce/drain 正常，acceptance smoke 再升到 **23/23 通过**。这一步的意义是：系统开始有 openhanako 那种“热态续接包”，但仍然明确只是 derived hot pack，不越权覆盖 source layer。
- 12:24～12:28：把 `session-pack` 生命周期真正接完整，而不是停在“能 build 一次”。新增 `session-pack-lifecycle.mjs`，支持 `build / refresh / finalize / rollover / expire / history`；`session-pack.mjs` 也补上 `status=active`。调度侧同步接入：`scheduler-plan.mjs` / `scheduler-run.mjs` 现在支持 `session-pack-finalize`（`session-end` 自动触发）、`session-pack-refresh`（`subagent-complete` 刷新 handoff 热态）、`session-pack-expire`（`heartbeat` / `daily` 做 TTL 维护）；`hook-dispatch.mjs` 透传 `sessionKey`；`compiler-status.mjs` 新增 `sessionPackState` 观察 current + history。整体验收新增一组 pack 生命周期检查：`session-pack-finalize-clears-current`、`session-pack-history-visible`、`session-pack-refresh-rebuilds`、`session-pack-expire-clears-current`、`session-pack-refresh-after-expire`、`scheduler-session-end-finalizes-pack`、`session-scene-fallback-after-finalize`、`session-pack-restored-post-finalize`。结果全部通过，acceptance smoke 提升到 **31/31 通过**。这意味着系统现在不只是“有热态 pack”，而是已有一条完整的 pack 生命周期：生成、被 runtime 使用、随事件刷新、会话结束时 finalize、TTL 到期时过期、历史可回查。
- 12:48～12:51：按“不停下来汇报”的要求继续往优先级前两项推进，补齐 session pack 的 handoff / thread 绑定 / runtime 分片。`session-pack.mjs` 新增 `threadBindings`（primary/secondary threads）、`unresolvedRisks`、`handoffDraft`；`runtime-selector.mjs` 的 `scene=session` 支持 `packVariant=brief|task|handoff`，可以按场景切不同热态包厚度；`session-pack-lifecycle.mjs` 新增 `action=handoff`，会把 handoff capsule 同时落为 `memory/compiler/session-packs/handoffs/*.json` 和 `*.md`；`scheduler-plan.mjs` / `scheduler-run.mjs` 新增 `subagent-complete -> session-pack-handoff`；`compiler-status.mjs` 现在额外暴露 `latestSessionHandoff`、当前 pack 的 `primaryThreadId/secondaryThreadIds` 与 `handoffCount`。验收补了 4 个新点：`session-pack-thread-binding-visible`、`session-brief-slice-tightens`、`session-handoff-slice-adds-capsule`、`scheduler-subagent-complete-builds-handoff`，全部通过，acceptance smoke 升到 **35/35 通过**。最后顺手把 `scripts/memory-compiler/` 下 `.tmp.accept-*.json` 临时文件清掉，工程卫生比前一轮干净一截。

- 12:53～13:06：把“主链路嵌入骨架”真正落到可跑代码：新增 `runtime-bridge.mjs`（selector + resume-handoff + review-triage 组合注入块）和 `review-triage.mjs`（review queue 排序摘要）。同时在 `plugins/memory-lancedb-pro` 增加 `derivedSessionBridge` 可选配置（默认关闭），支持在 `before_prompt_build` 直接注入 derived context，并在 `session_end` 自动产出 handoff capsule + finalize session pack。验收扩到 **38/38 通过**，说明 bridge/triage 这条主链路注入骨架也跑通了。
- 19:59～20:00：老板确认继续进入下一轮；当前默认推进目标切到“整体验收 / 冻结 / 提交”，仍按 P 级节奏整包收口，不做中途碎汇报。
- 20:16～20:18：当前轮已真正落下两笔提交：`plugins/memory-lancedb-pro` 子仓 commit `a6ba48d`（`feat: add derived session bridge hooks`），根仓 commit `09189af`（`feat: freeze memory compiler control plane and ops`）。同时补写 `reports/openclaw-memory-compiler/FREEZE-2026-03-20.md`，并把 `memory/compiler/` 运行态/派生产物及 `scripts/memory-compiler/.tmp.*.json` 临时验收文件加入根仓忽略，避免把运行态垃圾混入源码 freeze。
- 【heartbeat补记】20:20～20:24：按老板要求复扫并同步到 `laoda / lebang`。期间现场确认并修掉一个真实运维坑：`scripts/sync-to` 在“选定路径同步”分支未套用排除规则，导致同步 `plugins/memory-lancedb-pro` 这种嵌套仓时会把 nested `.git/` 一起带进 rsync，远端因此报错；现已补成统一使用排除规则。修复后重跑同步，`laoda` 与 `lebang` 两侧目标路径均已落地，`openclaw-gateway.service` 复验均为 `active`；其中 `lebang` 先前那次 `Connection reset by peer` 最终判断为重启后的瞬时验活抖动，不是同步内容缺失。
- 20:40：老板已拍板 `derivedSessionBridge` 走**方案 A：拆成独立 extension / bridge 插件**；拆之前参考 `VAN4622/openhanako` 的系统记忆设计思路，重点吸收其“memory core / bridge / scheduler-hub 分层”方法，而不是照搬具体实现。
- 【heartbeat补记】22:00～23:26：今晚继续按“老规矩”低噪音推进，没有插单改方向，主线稳定停在 memory compiler 的 review/fact lifecycle 治理收口。先后完成三段实锤进展：1) precise scene selector / runtime diagnostics 可观测性补强，commit `bdc4a81`（`feat: expose runtime scene diagnostics`），acceptance smoke 提升到 **52/52**；2) `reject` 从“借道 stale”改成有显式语义的 lifecycle 动作，打通 review queue → review-apply → fact state 全链，commit `1f6894e`（`feat: wire review reject lifecycle`），acceptance smoke 提升到 **56/56**；3) promotion policy 再收紧：blocked promotion 留痕、open review 持续待办、人工 override 路径与控制面 blocked 指标补齐，commit `6e26158`（`feat: harden promotion review policy`），acceptance smoke 再升到 **64/64**。
- 【heartbeat补记】23:26：这轮更像把 review/fact lifecycle 从“能跑”推进成“可治理、可观测、可验收”的阶段；当前下一刀自然落点已收敛到 **dispute flow / refresh policy / requeue policy**，不是另起炉灶。
