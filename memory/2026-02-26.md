# 2026-02-26 记忆记录

## 1. codex-deep-search 技能测试与修复
- 技能路径：`/root/.openclaw/workspace/skills/codex-deep-search/`
- **问题**：脚本中 `--colors=never` 参数不被系统 ripgrep 支持，报错
- **修复**：改为 `--color=never`，兼容旧版 ripgrep
- **测试结果**：用 `cliproxyapi/gpt-5.2` 做关键词搜索，成功命中 openclaw.json 里的配置证据
- **结论**：技能功能正常，可用于本机深搜

## 2. 记忆文件 GitHub 同步策略调整
- **原策略**：仅同步 `memory/topics/`，每日会话文件被 `.gitignore` 忽略
- **用户选择 B**：将 `memory/YYYY-MM-DD.md`（每日汇总）也同步到 GitHub
- **变更内容**：
  - `.gitignore` 新增例外规则：`!memory/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9].md`
  - 已推送文件：`memory/2026-02-24.md`、`memory/2026-02-25.md`
  - 保留忽略：`memory/2026-02-24-xxxx.md`（带时刻后缀的细粒度日志仍不推送）
- **目标仓库**：`git@github.com:2951461586/openclaw-memory-laoda.git`

## 3. memory-lancedb-pro 插件状态
- **安装路径**：`/root/.openclaw/workspace/plugins/memory-lancedb-pro`
- **模型支持**：`text-embedding-3-small` 已内置在 `EMBEDDING_DIMENSIONS` 表（1536维）
- **待配置**：OpenClaw 主配置中缺少 `memory.embedding` provider 区块
- **当前 provider 环境**：
  - `models.providers`：cliproxyapi、baiduqianfancodingplan（用于对话生成）
  - `memory.embedding`：未配置（用于向量存储检索）
- **可用选项**：
  - A) OpenAI `text-embedding-3-small` + OpenAI API Key
  - B) Jina `jina-embeddings-v5-text-small`（推荐，免费额度大）
  - C) 通过 cliproxyapi（如果支持 embeddings）
- **待办**：等待用户选择 provider 方案后补充配置

## 4. 当前模型状态
- **主模型**：`cliproxyapi/gpt-5.2`
- **回退**：`['cliproxyapi/gpt-5.2','cliproxyapi/gpt-5.3-codex','baiduqianfancodingplan/qianfan-code-latest']`
- **网关状态**：运行中，RPC probe ok
