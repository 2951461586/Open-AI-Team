# 2026-03-28

- Mulerun 本机号池代理在 Violet 继续推进 source-first 排障与可观测性增强：已补出 `GET /v1/pool/status` 状态接口，返回 provider 汇总、credential 脱敏状态、`rotation_mode`、`selection_mode`、`health`、`balance_state`、`last_failure` 等字段。
- 2026-03-28 午间实测结论：本机 `127.0.0.1:18000` 的 `mulerun` provider 当前 3/3 credential 均可被状态面识别为 `billing_exhausted` / `exhausted`，说明本机号池报 `Could not acquire a key for model mulerun/claude-opus-4-6 within the global time budget` 的根因不是并发锁占满，而是池内 key 余额已耗尽。
- 本轮为余额耗尽补了字符串兜底识别：即使底层 `last_failure.error_type` 仍落成 `unknown`，只要失败消息含 `insufficient_balance` / `Available balance` / `Minimum required` / `402 Payment Required` 等特征，也会在状态面标成 exhausted，避免只能靠超时报错猜原因。
- 号池调度入口已扩展支持 provider 级 `selection_mode` 环境变量（含 `least-inflight`），用于后续在多 key 仍健康时优化选 key 行为；但当前 mulerun 池因 3/3 余额耗尽，调度优化暂时没有实际发挥空间。
- 已尝试在 acquire 流程增加“全 key 最近都余额耗尽则直接 fail-fast”的逻辑，目标是把报错从 `global time budget` 收口成更准确的余额耗尽提示；截至本轮结束，状态面已生效，但请求侧 fail-fast 仍未完全验收通过，后续继续沿 source-first 路线查穿透链路。
- 过程性坑位：重启本地 18000 代理时出现过端口占用导致的新进程 bind 失败，但旧进程仍在对外服务；后续改这类本地代理要优先核对实际监听进程，再判断代码是否已真正热到运行态。
- 继续中的主线程仍是记忆编译/recall 升级：精确问答坚持 source-first，再补 precise scene selector，并继续检查 import 后 source backlinks、runtime source mix，以及 LCM / memory-lancedb 适配层。
- 2026-03-28 下午：号池先从 3 key 扩到 **5 key**，代理重启后实测本机 18000 `/v1/models` + `/v1/chat/completions` 均恢复可用。through-OpenClaw 子会话 `mulerun/violet-opus` 也已验证通过（9 秒 pong）。
- 2026-03-28 16:01-16:04：按用户要求再次对 `mulerun/violet-opus` 做配置面收口，`contextWindow` 已显式确认/重载为 `200000`。
- 2026-03-28 16:20+：继续把 `mulerun/violet-opus` 的运行时引用做最终态收口；Violet / laoda / lebang 三边都已确认保留 canonical ref，`agents.defaults.models["mulerun/violet-opus"]` 收口为 `{}`，不再保留 alias 尾巴，避免流式路径再把 `violet-opus` 当 lookup key。
- 2026-03-28 16:45+：本机 mulerun 号池继续扩到 **10 key**；本轮维护已完成追加、去重/排序/连续编号能力准备，但具体 key 明文不写入记忆。
- 同一轮已把本机代理配置拆成 **主配置 env + 独立 key 池 env**：`/root/.openclaw/mulerun-proxy.env` 只保留基础项，`/root/.openclaw/mulerun.keys.env` 单独承载 `MULERUN_API_KEY_*`；`mulerun-proxy.service` 已改为同时加载两份 `EnvironmentFile`。
- `mulerun.keys.env` 顶部已补维护模板：后续可直接把新 key 贴到文件末尾，再按需统一做去重 / 排序 / 连续编号。
- 已将 mulerun 号池代理完整链路（架构、部署细节、OpenClaw 配置面、验收记录、常见故障、扩容操作与本地维护约定）同步更新到 `memory/systems/openclaw-config.md`、`memory/systems/server.md` 与项目侧本地运维文档。
- 当前未完成待续：用户仍要继续推进 **P4 并发调优 + P5 健康度观测**；号池已完成本轮扩容与维护收口，后续重点转回观测与调度表现。
