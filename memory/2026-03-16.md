# 2026-03-16

- 已把 `github-trending-daily` cron（ID: `062e7113-fb05-4677-a7cc-3c0167b13f9d`）改为**固定输出纯 markdown 列表**：明确禁止卡片摘要风、`digest-card`、structured card、JSON 和额外前言；要求首行为标题，后续每个项目各占一行并以 `- ` 开头，包含仓库名 / 语言 / 今日星标 / 简介 / 链接。
- 【heartbeat补记】已确认 `laoda` 上最近“session 卡住”更像**默认模型链路阻塞**而非 session 引擎死锁：`cliproxyapi/gpt-5.4` / `gpt-5.2` 先后超时，且 LCM / `openai-responses` 路径出现 `401 Incorrect API key provided`，最后由 `baiduqianfancodingplan/qianfan-code-latest` fallback 成功，所以体感像卡住。
- 【heartbeat补记】已把 `opusproxy8069/claude-opus-4.6`（alias: `opus8069`）接到 `laoda` 与 `lebang` 作为**可切换模型**；两机 primary / fallback 都未改动，仅新增手动切换入口。
