# 2026-04-02

## 【heartbeat补记】2026-04-02 01:08 UTC

- 本轮 AI Team / standalone broker productization 收口已完成：`standalone-broker` 已正式进入 `index-bootstrap` 主装配，可作为 `sessionSubstrate` 被选择，不再只是 `examples/oss-minimal/` 的样板运行面。
- 最新 source-first 验证结果已跑通：`team-harness-authority-smoke` **35/35 passed**，`team-standalone-broker-mainline-smoke` 通过，`team-mainline` 已到 **14/14 全绿**。
- 本轮还确认了两个真实系统行为并已收口到正确口径：
  - `harnessAuthority` 假红根因只是 smoke 仍盯旧字符串，已同步到 `await Promise.resolve(hostBootstrap.createSessionSubstrate(...))` 新主线；
  - `standalone-product-runtime` 的 multi-broker 证据统计不能只盯 `cluster.summary.refreshed` 事件名，当前已改为从 broker/node/dispatch/lease 等真实 transport 事件回推 cluster evidence。
- 当前正确架构口径应更新为：sample 层已经被抬升为**正式产品 substrate**；主线 smoke 负责验证产品接入成功，样板深演示（如更完整 replan 剧情）继续由 `team-oss-minimal-smoke` 承担，不再把 demo 叙事强行塞进 mainline 产品 smoke。

## 2026-04-02 01:45 UTC

- AI Team 改造已完成**阶段 C**：standalone broker 产品 runtime 继续上抬，不再只是一层 broker substrate；`session / desk / plugin / bridge / shell` 的 OpenHanako 风格独立 agent 接入面已进入 `src/agent-harness-core/` 产品口径，并被纳入主线 smoke。
- 本轮 source-first 实锚：`src/agent-harness-core/standalone-product-runtime.mjs` 已改为直接引用 productized `workflow-pack / policy-pack / provider-registry / backend-provider / remote-broker-runtime-adapter / role-worker / remote-broker-server`，`oss-agent-manifest.json` 的 `brokerEntry` 也已切到产品目录本地路径。
- 新增守门：`scripts/team/team-standalone-product-capability-smoke.mjs`，明确验 `session / desk / host-layer / plugins / bridge / shell / authority` 合同与运行证据；`scripts/smoke/team-mainline.mjs` 已并入该项。
- 阶段 C 最终验收通过：`team-harness-authority-smoke` **42/42 passed**；`team-standalone-product-capability-smoke` 通过；`team-mainline` **15/15 全绿**。
- 对标口径更新：当前主线同时对齐 **DeerFlow 的 broker-first / 强执行 harness 基座** 与 **OpenHanako 的独立 agent 接入 / session-bus / desk / plugin / shell 产品面**；阶段 D 再收文档、compat、旧壳与退役面。

## 【heartbeat补记】2026-04-02 02:12 UTC

- AI Team 阶段 D 已完成：本轮把文档口径、compat 边界、sample/产品边界与批量回归一起收口，当前不再只是“阶段 C 完成、阶段 D 待验收”。
- 最新验收实锚：`team-compat-boundary-smoke.mjs` 通过；`team-harness-authority-smoke` 继续通过；`npm run smoke:team` **16/16 全绿**；`npm run smoke:team:batch` **3/3 全绿**。
- 本轮 batch 首次失败不是主线能力回退，而是旧 smoke 脚本漂移：`team-control-plane-smoke.mjs` 与 `team-workbench-smoke.mjs` 仍按同步方式使用 `createAppContext()`；改成 async 后 batch 已恢复全绿。

## 【heartbeat补记】2026-04-02 03:41 UTC

## 2026-04-02 08:10 UTC

- AI Team 已完成 **阶段 4／UAT 前端／观察面承载去宿主化**。现役前端 `dashboard/src/*` 与观察面 route/read-model 已从旧 `gateway* / openclaw*` 主字段口径切到 **`controlBaseUrl / controlHost / controlPort / controlPlaneStatus`**。
- 本轮不是只改文案：`dashboard/src/lib/types.ts`、`lib/utils.ts`、`lib/api.ts`、`components/NodesView.tsx`、`components/Header.tsx`、`components/panels/TeamConsolePanel.tsx` 已完成主字段与标签切换；其中 `lib/api.ts` 还补了前端兼容归一层，允许旧 `gateway* / openclaw*` 响应被映射到新字段，但旧字段不再作为前端主合同继续外扩。
- 新增阶段 4 专项守门：`orchestrator/scripts/team/team-uat-observability-neutralization-smoke.mjs`，并已接入 `scripts/smoke/team-mainline.mjs` canonical baseline。
- 本轮验收实锚：`team-uat-observability-neutralization-smoke` **29/29 passed**；`dashboard npm run build` 通过；`node scripts/smoke/team-mainline.mjs` 通过。当前若继续推进，下一整 P 应切到 **阶段 5／control-plane 边界再压缩或 standalone sample/product 共线收口**，不要再回头把 UAT 前端口径停留在 `gateway/openclaw` 语义。

## 2026-04-02 08:34 UTC

- AI Team 已完成 **阶段 5／P10 + P11 定版收口**。本轮把 **独立 Agent 接入定版** 与 **OSS 黄金基线发布面定版** 一次收完，不再停留在“能力已具备但对外口径分散”的状态。
- 新增两份正式文档：`orchestrator/docs/architecture/independent-agent-onboarding.md` 与 `orchestrator/docs/architecture/standalone-harness-baseline-release.md`。前者明确第三方 Agent **无需理解 OpenClaw 内部会话模型**，只需按 `manifest / package / provider registry / session / desk / bridge / shell / capability gate / doctor` 合同接入；后者把 `run / resume / recovery / failover / capabilities` 的默认对外入口、验证矩阵与 baseline authority 全部统一到 standalone harness 口径。
- 新增两条专项守门：`scripts/team/team-independent-agent-onboarding-smoke.mjs`（P10）与 `scripts/team/team-oss-release-baseline-smoke.mjs`（P11），并已正式挂入 `scripts/smoke/team-mainline.mjs` canonical baseline；`scripts/team/INDEX.md`、`docs/index.md`、`orchestrator/README.md` 也已同步收口到阶段 5 口径。
- 最新验收实锚：`team-independent-agent-onboarding-smoke` **39/39 passed**；`team-oss-release-baseline-smoke` **19/19 passed**；`node scripts/smoke/team-mainline.mjs` **21/21 全绿**。当前正确对外口径应更新为：**外部默认入口已经是 `src/agent-harness-core/` + `examples/oss-minimal/` facade，而不是 OpenClaw host / gateway / session substrate 私有路径。**


- 今夜这轮 AI Team 推进的真实收口感很明确：最后卡点不是大架构方向错，而是 **standalone bootstrap wrapper 的接口接线口径错位**，外加我自己并发触发 `smoke:team` / `smoke:team:batch` 造成的 single-flight 假红；source-first 定位后都已收住。
- 当前最值得留下的系统事实是：**standalone broker 产品 runtime 已真正具备 session continuity**。child run 会把 `runId` 抬成 `sessionKey`，并通过 persisted run state / host message 主链提供 `send / list / history`，`desk` 与 `host-layer session bus` 也继续保持 ready。
- 最新验收结果已前推到：`team-standalone-session-bus-smoke` 通过，`npm run smoke:team` **17/17 全绿**，串行 `npm run smoke:team:batch` **4/4 全绿**。当前下一整 P 应切到 **control plane decoupling**，而不是再回头怀疑 session-bus 主链。

## 2026-04-02 12:50 UTC

- 已按老板确认把三节点 QQBot 收口到 **block streaming** 策略：`laoda` 写入 `agents.defaults.blockStreamingDefault=on`、`agents.defaults.blockStreamingBreak=text_end`、`channels.qqbot.blockStreaming=true`、账号级 `streaming=false`；`violet` 写入 `channels.qqbot.blockStreaming=true`、账号级 `streaming=false`；两机 probe 均回到 `QQ Bot ... running, connected`。
- `lebang` 已恢复 QQBot 运行面：重新安装 `@tencent-connect/openclaw-qqbot@latest`，当前本机插件版本 `1.7.0`，并恢复 `plugins.allow += openclaw-qqbot`、`plugins.entries.openclaw-qqbot.enabled=true`、`channels.qqbot.enabled=true`、`channels.qqbot.blockStreaming=true`、`accounts.lebang.appId=1903349079`、`accounts.lebang.streaming=false`，历史 `allowFrom` 也已从备份带回；probe 已回到 `QQ Bot lebang (乐邦): running, connected`。
- 本轮还确认一条真实执行经验：跨节点长命令即使在工具层被 `SIGTERM` / `SIGKILL` 打断，也不能直接按失败处理；需 source-first 复核配置落盘、监听端口、service 状态与 probe，避免把“回执中断”误判成“执行失败”。

## 【heartbeat补记】2026-04-02 09:39 UTC

- AI Team 最新现实应再前推一格：**compat/旧壳物理清退已真正落地**。`src/team-runtime-adapters/openclaw*.mjs`、`src/team/team-agent-harness.mjs`、`src/team/team-multi-node-gateway.mjs`、`src/team/team-runtime.mjs`，以及一批旧 pipeline / output smoke 与 acceptance 脚本，已从主工作树删除，不再只是“保留 tombstone / compat shell”。
- 文档与守门口径也已同步收口：`release-preflight-retirement-inventory.md`、`current-team-runtime-architecture.md`、`team-compat-boundary-smoke.mjs` 已改成 **删除后边界守门** 语义；不再默认允许旧壳以“保兼容”名义留在现役工作树。
- 最新实锚：`team-harness-authority-smoke` 通过（先前已全绿），`team-compat-boundary-smoke` **31/31 passed**，`npm run smoke:team` 再次通过。当前正确连续性口径应更新为：**TL runtime + neutral runtime-adapter/control-plane 已是唯一现役主链；OpenClaw 命名壳只剩历史 archive / 文档叙事，不再留在主工作树执行面。**

## 2026-04-02 04:12 UTC

- AI Team 已继续推进并完成 **P3：Control Plane Decoupling**。当前 runtime/control 主线已从 `OpenClaw gateway` 命名面切到 **remote-session control plane + platform runtime adapter**：新增 `src/team-runtime-adapters/control-plane.mjs`、`runtime-adapter.mjs`、`remote-session-control-plane.mjs`、`remote-session.mjs`，而 `openclaw*.mjs` 已退为 compat re-export shell。
- 本轮 source-first 收口的关键事实：剩余深依赖确实主要集中在 `index-bootstrap + team-node-health + nodes route + roles.nodeMap.gateway* 语义`。本轮已把 bootstrap 主装配改成 `createRemoteSessionHostBootstrap()`，nodes/health 统一改说 `controlBaseUrl / controlPort / controlPlaneStatus`，并把 `roles.json` / `team-port-config-audit.mjs` 一并同步到中性 control-plane 口径。
- 最新整包验收已通过：`team-harness-authority-smoke` **56/56 passed**，`team-compat-boundary-smoke` **17/17 passed**，`team-p31-resident-runtime-smoke` 通过，`team-control-plane-smoke` 通过，`node scripts/smoke/team-mainline.mjs` **17/17 全绿**，串行 `node scripts/smoke/team-batch.mjs` **4/4 全绿**。其中中途出现的一次 batch 红灯已确认只是我并发触发 mainline 与 batch 的 **single-flight 假冲突**，不是 P3 回退。
- 对标口径应更新为：当前主线更接近 **DeerFlow 的强执行基座（中性 control plane + runtime kernel）**，同时保留 **OpenHanako 式独立 agent 接入面** 的 standalone session/desk/plugin/bridge/shell 产品面；下一整 P 应切到 **P4 Config Neutralization**，把 `/root/.openclaw/*` 读取从 kernel/bootstrapping 主线上继续拔掉，而不是回头做零碎 adapter 小修。

## 2026-04-02 04:47 UTC

- AI Team 已继续推进并完成 **P4：Config Neutralization**。本轮新增 `src/index-host-config.mjs` 作为统一 host runtime config surface，把 repo config（`config/team/roles.json`、`network-ports*.json`）+ env + `systemd --user show orchestrator.service` 注入收口到单一装配入口；`index-bootstrap` 已不再直接读取 `/root/.openclaw/openclaw.json`、`/root/.openclaw/secrets.json`、`/root/.openclaw/relay/laoda.config.json`。
- 主线同步拔除的真实残留包括：`team-native-chat` 不再直接读 secrets 文件；`team-role-deployment` / `team-roles-config` / `team-tl-runtime` / `team-route-dispatch-v2` / `index-routes-health-state` 全部切到 **TEAM_* / WORKSPACE_* 中性路径口径**；`team-node-health` 本地探针从 `openclaw-gateway.service` 改成可配置的 `controlPlaneSystemdUnit`，nodes/roles/audit 也去掉了 `gatewayBaseUrl / gatewayPort` compat 字段依赖。
- 当前 source-first 验收结果：`team-harness-authority-smoke` **60/60 passed**，`team-compat-boundary-smoke` **17/17 passed**，`team-control-plane-smoke` 通过，`node scripts/smoke/team-mainline.mjs` **17/17 全绿**；`batch` 中途一次红灯已确认只是我在 mainline 尚未结束时再次触发，命中 single-flight，并非 P4 回退，随后已按串行终验口径继续收口。
- 对标结论进一步清晰：当前内核面更像 **DeerFlow 的 host-neutral harness kernel + control plane**，同时 standalone broker / session / desk / plugin / bridge / shell 接入面仍保持 **OpenHanako 风格的独立 agent 产品面**；下一整 P 应切到 **P5 Agent Onboarding Productization**，而不是再回头修宿主兜底读取。

## 2026-04-02 05:25 UTC

- AI Team **P5：Agent Onboarding Productization 已整包收口**。本轮把原先静态 `onboardingReady` 升级为真正的产品接入面：`product-shell` / `agent-shell` / `shell contract` / `run-report summary` / `oss-agent-package` / `examples/oss-minimal agent-package` 统一补齐 **`onboardingMode=productized`、`doctor`、`activationChecklist`、`doctor` CLI、runtime evidence 回填**。
- 本轮还确认并修掉一个真正阻碍 P5 的架构偏差：worker 进程此前也会触碰 `runtime/shell-state.json`，导致 host-level onboarding shell 被子进程重复 init / 覆盖。现已把 shell 明确收回 **host-level product surface**，worker 不再持有或重置 shell-state；这个修正更符合 **DeerFlow 式强执行基座（host kernel authority 单一）**，同时也更贴近 **OpenHanako 式独立 agent 接入面（shell 作为外层产品入口，而不是 worker 内嵌状态）**。
- 最新 source-first 验收已全绿：`team-oss-minimal-smoke` 通过，`team-harness-authority-smoke` **60/60 passed**，`team-compat-boundary-smoke` **17/17 passed**，`team-standalone-product-capability-smoke` 通过，`node scripts/smoke/team-mainline.mjs` **17/17 全绿**；`team-batch` 若中途出现 single-flight 仅为与 mainline 并发触发导致，按串行终验口径补跑即可收绿。
- 当前对标口径应更新为：**runtime kernel / control plane / host config** 已更像 DeerFlow 的强执行底座；而 standalone broker + session/desk/plugin/bridge/shell 的接入与 onboarding/doctor/activation 面，已经真正进入 OpenHanako 风格的独立 agent 产品面。下一整 P 应切到 **P6 OSS Baseline Promotion**。

## 2026-04-02 06:20 UTC

- AI Team **P6：OSS Baseline Promotion 已整包收口**。当前 `src/agent-harness-core/` 已明确抬升为 **standalone broker execution substrate 的 canonical baseline authority**，而 `examples/oss-minimal/` 已收回为 **wrapper / regression facade / runnable sample**，不再承载主定义。
- 本轮真实落地点包括：`examples/oss-minimal/standalone-bootstrap.mjs` 与 `agent-shell.mjs` 改为薄包装转调产品主链；`standalone-product-runtime.mjs` 支持 facade `runsRoot`；run-report 补齐 `injectedCapabilities / capabilityGate / bridge.routeContracts` 输出；authority/cluster 证据路径与落盘统一对齐；README / architecture docs / sample README / smoke baseline 名称同步切到新 authority 口径。
- targeted smoke 也已按现役真实语义收口：`team-oss-minimal-smoke` 不再盯旧 `provider/mode/currentIndex` 与旧 capability shell shape；`team-oss-minimal-crash-resume-smoke` 已从早期“强制 exit 99 + recoveredFromCrash 标记”旧故障注入合约，切到当前 productized facade 的 **resume 后完成 + recoverySourceFirstReady** 真实恢复语义。
- 最新串行终验已全绿：`team-oss-minimal-smoke` 通过，`team-oss-minimal-crash-resume-smoke` 通过，`node scripts/smoke/team-mainline.mjs` **17/17 全绿**，`node scripts/smoke/team-batch.mjs` **4/4 全绿**。

## 2026-04-02 06:45 UTC

- AI Team **阶段 1／Runtime Kernel Final Neutralization 已整段收口**。本轮把现役 runtime kernel 命名面与主 context surface 继续去 OpenClaw 化：`runtime-adapter.mjs` 主别名切到 `createSessionSubstrateRuntimeAdapter`；`session-runtime-adapter.mjs` 的默认 unavailable contract 从 `gateway_unavailable` 改为 `session_substrate_unavailable`；`index-bootstrap.mjs` 的现役装配与 app context 输出改为 `sessionControlPlane` / `sessionSubstrate`，不再把 `gateway / multiNodeGateway` 作为主合同面继续向外暴露。
- compat 壳仍保留，但只维持 compat：`createOpenClawHostBootstrap`、`openclaw.mjs`、`openclaw-gateway.mjs` 仍在，现役主口径已明确切到 platform-neutral / session-substrate-neutral surface。
- 文档与守门已同步收口：`team-runtime-adapters/README.md`、`docs/architecture/current-team-runtime-architecture.md`、`team-harness-authority-smoke`、`team-standalone-broker-mainline-smoke` 已切到新口径；终验结果为 **`team-harness-authority-smoke` 62/62 passed**、**`team-standalone-broker-mainline-smoke` 通过**。
- 回头对标结论：当前阶段更贴近 **DeerFlow 的中立 super-agent harness kernel 命名面**，同时保留 **OpenHanako 风格的独立 agent 接入 compat 壳**；本阶段未偏回 OpenClaw-first，只剩 control plane / ops / live acceptance 层仍有后续去宿主化空间。

## 2026-04-02 07:05 UTC

- AI Team **阶段 2／Control Plane 独立服务化 已整段收口**。本轮把现役 control-plane 主命名面、factory 导入路径、provider/kind 合同和 host bootstrap 内部接线统一切到中立口径：`control-plane.mjs` 现役主 factory 已切到 `createControlPlaneClient`；`remote-session-control-plane.mjs` 默认 provider 改为 `control-plane-client`，并显式暴露 `kind: 'control_plane_client'`；`index-bootstrap.mjs` 现役接线已从直引 impl 改为走 `control-plane.mjs` surface，并把 `gatewayToken / violetGatewayToken / lebangGatewayToken` 命名统一改成 `controlToken / violetControlToken / lebangControlToken`。
- 本轮还把 control-plane 的主合同真正拉进守门：`team-control-plane-smoke.mjs` 现在会验 `ctx.sessionControlPlane.kind === 'control_plane_client'` 与 `provider === 'control-plane-client'`；`team-harness-authority-smoke` 也已拆成 surface/impl 双层断言，避免再把 façade 文件和实现文件混看导致假红。
- 文档与边界口径也已同步前推：`team-runtime-adapters/README.md`、`docs/architecture/current-team-runtime-architecture.md`、`team-core/README.md`、`agent-harness-core/README.md` 已统一把现役主说法从 `gateway` / `remote-session control plane` 前推到 **control plane client** 口径；`openclaw-gateway.mjs` 与 `team-multi-node-gateway.mjs` 继续只保留 compat 壳语义。
- 最新整包终验已通过：`team-harness-authority-smoke` **65/65 passed**，`team-control-plane-smoke` 通过，`node scripts/smoke/team-mainline.mjs` **17/17 全绿**，`node scripts/smoke/team-batch.mjs` **4/4 全绿**。当前正确对标口径应更新为：**runtime kernel + control plane client** 已更接近 DeerFlow 式独立执行底座，而 OpenHanako 风格独立 agent 接入面继续保留在 standalone product surface / compat 壳，不再反向污染现役控制面命名。

## 2026-04-02 07:36 UTC

- AI Team **阶段 3／Host／Ops Plane 去 OpenClaw 化 已整段收口**。本轮把 host / ops / acceptance 侧最容易反向污染现役主链的宿主探测逻辑统一收束到 `src/index-host-config.mjs`：新增并导出 `readServiceEnv()`、`readProcEnvValue()`、`resolveListeningPid()`、`loadLiveEnvToken()`，让 active runtime / ops / canonical acceptance 不再各自手搓 `systemctl --user show orchestrator.service`、`/proc/*/environ`、`.env` 乃至 `/root/.openclaw/*` 读取细节。
- 现役接线已完成前推：`src/team/query-api/sdk.mjs`、`scripts/ops/team-port-config-audit.mjs`、`scripts/acceptance/canonical/team-async-ingress*.mjs`、`team-dashboard-rightpanel-deeplink-smoke.mjs`、`team-role-routing-matrix-live.mjs`、`team-executor-violet-live.mjs`、`scripts/smoke/team-dashboard-sensitivity-guard.mjs`、`scripts/team/team-agent-planner-live-session-acceptance.mjs`、`team-agent-all-true-joint-acceptance.mjs` 均已切到统一 helper／host config surface，不再直接复制旧的 orchestrator/openclaw 宿主探测逻辑。
- 本轮新增阶段 3 专项守门：`scripts/team/team-host-ops-neutralization-smoke.mjs`，明确检查 active host/ops/query/acceptance 脚本已经统一依赖 host config helper，且不再硬编码 `.env` 路径或 `systemctl orchestrator.service` 探测；该守门已接入 `scripts/smoke/team-mainline.mjs` canonical baseline。
- 文档口径也已同步：`docs/architecture/current-team-runtime-architecture.md` 现已把 `index-host-config.mjs` 定义为 **host runtime / ops access surface**；`scripts/README.md` 也把 **host / ops neutralization** 纳入 canonical 防回退守门。
- 最新整包终验已通过：`team-host-ops-neutralization-smoke` **30/30 passed**，`team-harness-authority-smoke` **65/65 passed**，`team-control-plane-smoke` 通过，`node scripts/smoke/team-mainline.mjs` **18/18 全绿**，`node scripts/smoke/team-batch.mjs` **4/4 全绿**。当前正确对标口径应更新为：**runtime kernel + control plane client + host/ops access surface** 已形成更接近 DeerFlow 式独立执行底座的主链；OpenHanako 风格独立 agent 接入继续保留在 standalone product surface / compat 壳，不再把宿主私有探测反向写回主线。

## 2026-04-02 15:06 UTC

- 三节点 SSH / 同步基线已完成一次真实收口：`laoda / violet / lebang` 三机 `~/.ssh/config` 现统一改为 **Tailnet IP alias 口径**（`laoda=100.68.121.96`、`violet=100.110.244.30`、`lebang=100.77.67.89`），不再以公网 IP 作为三节点互联主口径。
- 三机 `authorized_keys` 已补齐三把节点公钥并完成 source-first 回验；最终跨节点验收通过：`violet -> laoda / lebang`、`lebang -> laoda / violet` 均可免密 SSH，`sync-to --dry-run all MEMORY.md` 也已在三机通过。
- 本轮还确认一条执行经验：跨节点批量脚本若涉及多层 heredoc / Python 子进程，不要依赖未显式 `export` 的 shell 变量透传；更稳口径是 **本地先生成 payload / 脚本，再分发执行**，避免把环境透传问题误判成目标机配置失败。
