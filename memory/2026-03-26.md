# 2026-03-26

- memory-compiler / source-first recall: confirmed `import-real-sources` does accept LCM summary adapter input in both `inline` and `live-export` modes; earlier confusion was caused by `memory/compiler/reports/real-import.latest.json` being a rolling latest snapshot rather than operator live truth.
- Patched `plugins/memory-compiler/scripts/memory-compiler/import-real-sources.mjs` to emit `reportScope/truthMode` and split operator live vs acceptance latest outputs (`real-import.live.json`, `real-import.acceptance-latest.json`), and patched `control-plane-verify.mjs` to prefer `real-import.live.json` over `real-import.latest.json`.
- Patched `plugins/memory-compiler/scripts/memory-compiler/source-discipline-check.mjs` so direct CLI + wrapper calls both accept optional JSON payload input instead of erroring on any argv.
- Patched `plugins/memory-compiler/scripts/memory-compiler/lib/runtime-bridge-core.mjs` for cross-conversation LCM recall: when dispatching `lcm_expand_query` from `sum:*` refs, runtime now resolves and injects the correct `conversationId` from `~/.openclaw/lcm.db`; this fixed chat-side cross-conversation `summaryIds` recall.
- Extended runtime source dispatch for query-only recall: when no summary refs are already selected, runtime now performs local LCM DB discovery by query terms, picks candidate `summaryIds` + `conversationId`, and dispatches scoped `lcm_expand_query`; fallback tool naming was cleaned from obsolete `memory_recall` to `lcm_grep` with `allConversations: true`.
- End-to-end validation completed on 2026-03-26: (1) scoped `lcm_expand_query(summaryIds=["sum_4e6742ae47a210c5"], conversationId=212, ...)` succeeded; (2) query-only discovered summaries in conversation 219 and successfully answered via scoped `lcm_expand_query`; (3) runtime source-dispatch output now shows the injected `conversationId` and no longer emits `memory_recall`.
