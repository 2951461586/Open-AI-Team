# 2026-04-01

- Mulerun proxy native Anthropic usage accounting was patched in `services/mulerun-proxy/LLM-API-Key-Proxy/src/rotator_library/client.py`:
  - non-stream `/v1/messages` responses now convert Anthropic `usage` into the internal `record_success()` shape;
  - stream responses now defer success accounting until stream end, aggregate usage from `message_start` / `message_delta`, then release the key.
- Local validation on Violet after restart: native Anthropic `/v1/messages` non-stream and stream both returned `200` with usage present; `key_usage.json` continues tracking `mulerun/claude-opus-4-6` with `success_count / prompt_tokens / completion_tokens`.
- Cross-node routing check:
  - `laoda` and `lebang` can both reach Violet's mulerun proxy directly and get `200` on minimal Anthropic Messages smoke tests.
  - The real drift is on `laoda` CPA `8317`: requests for `claude-opus-4-6` return `unknown provider for model claude-opus-4-6` because the `claude-api-key -> violet mulerun proxy` block in `/root/cliproxyapi/config.yaml` is currently commented out/disabled.
- Durable takeaway: Violet mulerun proxy itself is healthy; the remaining issue is upstream consumer-side config drift on laoda CPA, which also affects lebang when it relies on that path.
- Cross-node Opus routing was explicitly re-cut on both `laoda` and `lebang`: removed `cliproxyapi` model entry `claude-opus-4-6-thinking`, then aliased `claude-opus-4-6-thinking -> mulerun/claude-opus-4-6`, and restarted `openclaw-gateway.service` on each host.
- Post-change steady state: `laoda` / `lebang` Opus traffic now goes directly to Violet mulerun proxy (`:18000`) instead of relying on laoda CPA `:8317`; host default primary model remains `cliproxyapi/gpt-5.4`.
- Local main-session default allowlist confirmed from `/root/.openclaw/openclaw.json`: `cliproxyapi/gpt-5.4`, `cliproxyapi/gpt-5.2`, `mulerun/claude-opus-4-6`, `baiduqianfancodingplan/qianfan-code-latest`.
- Local effective provider model set now includes only one Claude path on this host: `mulerun/claude-opus-4-6` (old local alias-facing entry `claude-opus-4-6-thinking` is no longer a standalone default model entry).
- 三节点记忆系统 source-first probe（violet / laoda / lebang）结论：若只看“主控面 / 插件代码 / 服务可用性”，三台已基本对齐——`memory-lancedb-pro` / `memory-compiler` / `lossless-claw` 均启用，相关插件代码 hash 一致，gateway 均 `active + healthy`。
- 但仍存在配置漂移：`memory-lancedb-pro` 的 recall 细参数并未完全统一；其中 `laoda` 当前显式带有一组 auto-recall 相关阈值，`violet` / `lebang` 更接近简化配置口径。因此“功能开关一致”不等于“细参数一致”。
- 冷层判断应区分“核心控制面产物”与“衍生物”：三台 control-plane 核心指标（counts / acceptance / sourceLinks 汇总）一致，可视为已同步；但 `digests.fileCount` 与 demo LCM summary 实物数仍有差异，说明冷层衍生物未完全收平。
- 热层 LCM (`lcm.db`) 当前不是三台同库镜像：三节点 `messages/summaries` 行数与 DB 体量差异显著，更像各节点本地会话热数据各自积累；不要把它误判成“应天然同态”。
- 用户已明确当前优先级：目标只收口“主控面 / 插件代码 / 服务可用性”；下一步先收平配置漂移，但**在收到明确指令前不重启 gateway**。
