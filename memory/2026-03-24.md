# 2026-03-24

## 【heartbeat补记】
- 05:00（Asia/Shanghai）心跳巡检：当前节点身份仍为 薇尔莉特（Violet）；主会话工作连续性以 memory-compiler / source-first recall 收口为主。
- 今日 daily memory 原先不存在，已按约定创建；当前未见需要额外外发提醒或重型巡检的事项。

- 10:16：老板反馈自动更新到 OpenClaw 2026.3.22 后因“官方分离插件 SDK”导致服务起不来，现已人工回退到 2026.3.13，并要求先做 doctor/status 体检。现场 source-backed 结果：当前 `openclaw --version = 2026.3.13`；`openclaw-gateway.service` 为 `active/running`，`rpc.ok=true`，监听 `127.0.0.1:18789`，说明回退后主服务已恢复。当前暴露的是非致命隐患：gateway service 仍引用 nvm Node 路径（升级后易再炸）、系统级 Node 22/24 缺失、`memory-lancedb-pro` 有 typed hook warning、`lossless-claw` provenance warning、默认 agent 缺 embedding provider/API key 导致 memory search 不可用、QQ 群 allowlist 为空会静默丢弃群消息。`openclaw update status` 还显示 stable 有 `2026.3.23-1` 可更新；在本次升级事故根因和自动更新策略没收口前，不应直接再跟进升级。
- 10:36+：老板明确要求先做两件事：1) 冻结自动更新；2) 把 gateway service 从 nvm Node 迁到系统 Node。
- 10:41+：两件事都已实做落地并复核通过：`openclaw.json` 已新增/确认 `update.channel=stable`、`update.checkOnStart=false`、`update.auto.enabled=false`，即后续不再自动升，也不在启动时主动做升级检查提示；同时已将当前可用的 `openclaw@2026.3.13` 复制到 `/usr/local/lib/node_modules/openclaw`，并把 `openclaw-gateway.service` 的 `ExecStart` 改到 `/usr/bin/node /usr/local/lib/node_modules/openclaw/dist/index.js gateway --port 18789`。现场 `systemctl --user show` 显示 `ActiveState=active` / `SubState=running`，`curl http://127.0.0.1:18789/health` 返回 `{"ok":true,"status":"live"}`。结论：gateway service 已脱离 nvm 安装目录依赖并保持健康；手动更新仍可做，但不会再自动升。
- 10:56+：顺手把“默认 CLI 仍先撞到 nvm”这条尾巴也抹了：`/usr/local/bin/openclaw` 已改成显式 wrapper（`/usr/bin/node /usr/local/lib/node_modules/openclaw/openclaw.mjs`），并删除 `/root/.bashrc` 里把 nvm PATH 强塞到最前的那条 export。用干净 login shell 验证后，默认已是 `openclaw=/usr/local/bin/openclaw`、`node=/usr/bin/node`。同时确认 `openclaw-auto-update.timer` 为 `disabled/inactive`。当前只剩一条 recommended 级审计提示：`gateway status` 仍说 service PATH 缺 `/root/.nvm/current/bin`，更像旧 install-path 审计口径残留，不影响实际运行与默认命令路径。
- 12:19+：按老板要求核查/回退 `lebang` 侧 OpenClaw。现场确认起始态为 `OpenClaw 2026.3.22`，且 `openclaw-auto-update.timer` 仍为 `enabled/active`。随后按老板指定三步执行：`npm uninstall -g openclaw`、`npm cache clean --force`、`npm install -g openclaw@2026.3.13`。结果要分层说清：默认 CLI 已变为 `/usr/bin/openclaw` 且 `openclaw --version = 2026.3.13`，`/usr/lib/node_modules/openclaw/package.json` 也为 `2026.3.13`；同时 `openclaw.json` 已写成 `update.channel=stable`、`update.checkOnStart=false`、`update.auto.enabled=false`，`openclaw-auto-update.timer` 现为 `disabled/inactive`。但这次重装链在验收尾声报过一次 `code 127`，根因不是安装失败，而是脚本还按 `/usr/local/bin/openclaw` 去验收；实际 npm 全局前缀已变为 `/usr`，所以命令入口落在 `/usr/bin/openclaw`。更关键的是：当前运行中的 `openclaw-gateway.service` 仍指向 nvm 下的 `2026.3.22` 路径，虽然服务本身 `active/running` 且 `health` 返回 live。也就是说，`lebang` 当前已完成“CLI 回退 + 自动更新冻结”，但 **daemon 运行位还没跟着切到 3.13**，后续还需补一刀 service 运行位统一。- 12:23+：继续把 `lebang` 侧 OpenClaw 回退收口补完。最终现场结果：默认 CLI 已变为 `/usr/bin/openclaw`，`openclaw --version = 2026.3.13`；`openclaw.json` 中 `update.channel=stable`、`update.checkOnStart=false`、`update.auto.enabled=false` 已落盘，`openclaw-auto-update.timer` 为 `disabled/inactive`；`openclaw-gateway.service` 当前 `ExecStart=/usr/bin/node /usr/lib/node_modules/openclaw/dist/index.js gateway --port 18789`、`OPENCLAW_SERVICE_VERSION=2026.3.13`，服务 `active/running`，`127.0.0.1:18789` 监听正常，`health` 返回 `{"ok":true,"status":"live"}`。本轮中途两次报错都不是 lebang 根因：一次 `code 127` 是验收脚本误查 `/usr/local/bin/openclaw`，一次只读复核报错是本地 `tr` 小命令写错。当前结论：`lebang` 已完成 **3.13 回退 + 自动更新冻结 + daemon 运行位统一**。
- 12:27+：按老板要求查看本机 doctor 状态，并顺手按运维模板补跑 `status --deep / security audit --deep / gateway status --json / doctor`。现场确认本机 gateway 运行健康：systemd `enabled/running`，`rpc.ok=true`，监听 `127.0.0.1:18789`。doctor 主要问题集中在配置/审计面：QQ `groupPolicy=allowlist` 但 allowlist 为空（群消息会静默丢弃）、gateway token 走 SecretRef 但当前命令路径拿不到导致 deep probe 报 auth unresolved、17 个 orphan transcript、默认 agent 缺 embedding provider/API key 导致 memory search semantic recall 不可用；插件面还有 `memory-lancedb-pro` typed hook warning 与 `lossless-claw` provenance warning。补充：`security audit --deep` 额外报 2 个 critical（`qqbot` 插件、`tavily-search` 技能命中 code safety 规则）和若干 warn（`gateway.tools.allow` 含 `sessions_spawn`、credentials 目录权限 755、部分插件安装未 pin / 缺 integrity）。结论：本机主服务健康，但 doctor/security 仍有一批可继续收口的风险项。
- 12:42+：继续处理本机 doctor 里最直接的两项：QQ 群 allowlist 与 credentials 权限。最终已把 `/root/.openclaw/credentials` 收紧为 `700`，并在 `channels.qqbot.accounts.default.allowFrom`、`channels.qqbot.accounts.violet.allowFrom` 中加入当前老板 QQ sender ID。过程中踩到一个明确坑：doctor/文档文案提示 `channels.defaults.groupAllowFrom/allowFrom` 可用于 group allowlist，但当前本机核心 schema 实际不接受这两个键；若硬加反而会把 config 弄 invalid。最后通过移除伪触发项 `channels.defaults.groupPolicy=allowlist`，doctor 已不再报 channel security 警告。当前剩余主要是 gateway SecretRef、orphan transcripts、memory search embedding auth 与插件 provenance/typed-hook 警告。
- 12:44+：补一条可复用结论，防止后续再踩同坑：本机这版 OpenClaw 在 QQ 群 allowlist 场景下，**不能**照 doctor/通用文档去写 `channels.defaults.groupAllowFrom` 或 `channels.defaults.allowFrom`；当前真实可用落点是 `channels.qqbot.accounts.*.allowFrom`。若继续保留 `channels.defaults.groupPolicy=allowlist`，会制造 schema/documentation 不一致导致的伪告警；本机已通过移除该默认项并改走 QQBot 插件自身 `allowFrom` 收口。
- 12:57+：继续处理本机 `gateway SecretRef / auth unresolved`。已把 `secrets.providers.filemain.timeoutMs` 从 `5000` 提到 `30000` 并重启 gateway。重启后现场证据：systemd `active/running`，`/health` 返回 live，`gateway status --json` 仍 `rpc.ok=true`。最关键变化是：`openclaw secrets audit --check` 已从之前 `unresolved=4` 变成 `unresolved=0`，说明 file SecretRef 在 CLI/read-only 路径里已不再超时；`gateway probe` 也已从 `device identity required` 变成 `Connect: ok`，只剩缺 `operator.read` scope 的 limited 提示。当前残留异常只剩 `openclaw doctor` 仍报 “Gateway token is managed via SecretRef and is currently unavailable”，与最新 `secrets audit`/`gateway probe` 实测不一致，更像这版 doctor 的 SecretRef command-path 残留误报，不再视作真实阻塞。
- 13:00+：继续处理 `agents/violet/agent/models.json` 的明文 API key 残留。已确认源配置里的三家 provider（`baiduqianfancodingplan`、`cliproxyapi`、`mulerun`）本身都已是 `filemain` SecretRef，脏的是 agent 生成物。最终没有手工抠明文，而是走 OpenClaw 官方定点重生：设置 `OPENCLAW_AGENT_DIR=/root/.openclaw/agents/violet/agent` 后执行 `openclaw models status --json`，触发目标 agent 的 `models.json` 重写。结果三家 `apiKey` 都被刷新成 `secretref-managed`，`secrets audit --check` 也已不再对该 `models.json` 报 `PLAINTEXT_FOUND`。
- 13:11+：处理本机 `~/.openclaw/agents/violet/sessions` 的 17 个 orphan transcript files。先确认 `doctor` 的 orphan 只统计未被 `sessions.json` 引用的 primary `.jsonl`，且 `openclaw sessions cleanup --enforce` 不会碰这批文件。最终按 doctor 自己的归档规则把这 17 个 `.jsonl` 重命名为 `.deleted.<timestamp>`，没有硬删；包含 `derived-bridge-e2e.jsonl`、`probe-mulerun-*.jsonl` 等测试残留。复核后 `openclaw doctor` 已不再提示 orphan transcript warning。
- 13:1x：继续清 doctor 剩余项。已确认 `Gateway auth` 那条 “Gateway token is managed via SecretRef and is currently unavailable” 不是 secret 丢失，而是 `openclaw doctor` 当前检查链没有先解析 `gateway.auth.token` 的 SecretRef；`gateway status --json` 与 `secrets reload` 都已验证 gateway 与 filemain secret source 正常。
- 13:1x：已确认 native `Memory search` 当前只是开了 `enabled=true`，但没有配置任何受支持的 embedding backend；现有 `models.providers` 只有 `baiduqianfancodingplan`、`cliproxyapi`、`mulerun`，不属于 memorySearch 支持集合，所以 doctor 才会挨个报 `openai/google/voyage/mistral` 缺 key。
- 13:33+：老板明确要求关闭 native memorySearch，已执行 `agents.defaults.memorySearch.enabled=false`。原因：native memory search 未配置受支持 embedding backend，持续触发 doctor 告警；当前记忆主链路继续使用 `memory-lancedb-pro + LCM + memory-compiler`。
- 13:5x：继续清 doctor 的 plugin diagnostics。采取最小风险方案：`memory-lancedb-pro` 去掉会触发 `unknown typed hook` warning 的 `registerHook(command:new/reset)` 路径；`lossless-claw` 通过显式 `plugins.load.paths` 补 provenance，再复跑 doctor 验证。
- 13:59+：确认 `memory-lancedb-pro` 两条 `unknown typed hook` warning 的真实根因：当前 OpenClaw 里 `api.on` 是 typed hook 注册入口，不是 internal hook event bus；`command:new/reset` 这类 internal hook key 应通过 `api.registerHook` 注册。
- 14:11+：继续收 startup optimization。为 root shell 与 `openclaw-gateway.service` 同步补 `NODE_COMPILE_CACHE=/var/tmp/openclaw-compile-cache`、`OPENCLAW_NO_RESPAWN=1`，避免只修 doctor 当前进程、不修真实 daemon/CLI 启动路径。
- 14:30+：已确认 `doctor` 的 `gateway.auth.token SecretRef unavailable` 是 CLI/doctor 路径问题：它对 gateway auth surface 走本地 `resolveSecretInputWithEnvFallback`，不走 active gateway `secrets.resolve`；当前进程若无 `OPENCLAW_GATEWAY_TOKEN`，即便 secrets 文件里 token 实际存在且 gateway 正常运行，也会误报。实测把同值注入 `OPENCLAW_GATEWAY_TOKEN` 后，warning 立即消失。
- 14:40+：按方案 A 落地 doctor 规避：新增 `/root/bin/openclaw-doctor-safe`，仅在执行 `openclaw doctor` 时临时从 `/root/.openclaw/secrets.json` 读取 gateway token 并注入 `OPENCLAW_GATEWAY_TOKEN`，不做长期 shell export。
- 14:44+：补充 shell alias：`odoc='/root/bin/openclaw-doctor-safe'`，新 shell 可直接运行 `odoc`。
- 15:23+：老板明确同意按既定顺序直接落地 memory-compiler 的“两刀”调整：1) `sum:` 仅在 LCM summary id 为 canonical 形态时才计入 trusted（用于挡住 `sum_demo_lcm_001` 这类 demo / placeholder 引用）；2) durable memory 导入 ref 统一收敛到 canonical `mem:<id>`，不再继续扩散 `mem:<plugin>:<id>` 形态。
- 15:40+：已在 `source-discipline` / `runtime-selector-core` / `digest-compiler` / `source-discipline-check|enforce` / `lancedb-memory-import` 上完成首轮补丁并过语法检查；期间踩到一次 `runtime-selector-core` 残留引用名不一致（`isTrustedRef` -> `isTrustedSourceRef`）导致 runtime probe 报错，随后已修正。修复后 `runtime-probe` 现场恢复 `ok=true`，precise 探针的 source mix 回到 `trusted-clean`，说明 demo sum ref 不再被误算为 trusted。
- 18:5x-21:0x：按老板要求把 memory-compiler 的 workspace-root legacy surface 做正式收口：本机先提交 `994c220 chore: retire legacy memory-compiler surfaces`，删除 `scripts/memory-compiler/`、`reports/openclaw-memory-compiler/`、`plugins/memory-compiler-bridge/`，并同步更新 `memory/projects/memory-compiler.md` 与 `skills/memory-compiler-ops/*` 的入口口径。随后定向同步到 `laoda`、`lebang`，两侧都完成“文件更新 + 旧目录清理 + 轻验活”；之后本机已 push 到 GitHub `origin/main`，两侧 `openclaw-gateway.service` 均 `active`，`/health` 均返回 `{"ok":true,"status":"live"}`。结论：这轮 plugin-first / legacy-retirement 收口已在三节点落地，运行面保持低扰动健康。
