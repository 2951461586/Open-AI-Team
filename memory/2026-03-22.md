# 2026-03-22

- 【heartbeat补记】00:00 之后主线仍连续，没有切题：memory-compiler 继续沿插件化与即插即用收口推进。当前气氛是“少说多做、能验就验”，不是新开战线。
- 【heartbeat补记】已跨到 3 月 22 日，新一天的 daily memory 已开档，避免上下文只挂在 3 月 21 日长条记录里。当前连续性仍以 `SESSION-STATE.md` 为准；若后面继续推进 GitHub 建仓 / legacy retirement / plugin doctor 安装体验，应优先把真实结果写到今天这页，而不是再往昨天的长日志里硬塞。
- 【heartbeat补记】凌晨这段已有几条值得留下的真实结论：1) `memory-compiler` 插件侧的 legacy retirement 已推进到 `preflight-only`，默认 docs 已切到 plugin-owned `plugin:docs`；2) bridge 在代码职责上已基本被本体 hook 替代，但**本机运行态实际上还没加载 `memory-compiler` / `memory-compiler-bridge`**，所以当前更准确的判断是“bridge 已可停、但生产切换未正式完成”；3) GitHub 建仓尝试踩到 token 401，说明现有那枚 `ghp_...` 凭据不可直接复用。
- 14:xx：老板明确要求按 1→2→3→4→5 顺序推进 memory-compiler 插件化收口（老规矩：少说多做、按顺序落地、统一回执）。
- 14:xx：已补齐 plugin 侧缺失脚本（原本仅存在于 `scripts/memory-compiler/` 的：`fact-arbitrate/dedupe/reconcile/repair-ids`、`ingest-normalize`、`runtime-source-mix-before-after`、`source-discipline-enforce`、`thread-cluster-apply`、`thread-lifecycle`），并把 workspace 同名脚本改为 compat wrapper（`runPluginCompatCli`）。
- 14:xx：plugin CLI 已补齐上述命令路由，`plugins/memory-compiler/test/cli-smoke.mjs` + `node --test` 全绿（16/16）。
- 14:xx：三节点配置快照：violet 已启用 `memory-compiler`（plugin-preferred），未启用 bridge；laoda/lebang 的 `plugins.load.paths` 目前仅含 `memory-lancedb-pro`，未启用 `memory-compiler`/bridge（需要后续按第 3 步继续确认是否要启用/验活）。
- 14:4x：第 3 步已真正收口：`laoda` / `lebang` 都已切到 `memory-compiler` 本体直连（`plugins.allow/load.paths/entries` 已补齐，`memory-compiler-bridge=null`），并确认 `openclaw-gateway` 在两台上是 **systemd --user** 服务，`systemctl --user is-active openclaw-gateway.service = active`；先前 plain `systemctl` 看见的 `inactive` 属于查错 scope，不是运行态没起来。
- 14:4x：第 5 步按“非破坏性收尾”推进：已在 `plugins/memory-compiler/src/commands.ts` 与 `docs/RETIREMENT-PLAN.md` 明确补上 `systemd --user` 提示，避免以后把 user unit 误判成 system-wide inactive；随后重跑 plugin-preferred `doctor/refresh/verify` 做最新证据链落盘，当前口径仍为 `trustLevel=trusted`、`operatorVerdict=trusted-with-acceptance-samples`，legacy 目录继续按 `defer/blocked` 保留，未做删除。
- 16:1x～16:2x：补记这轮收口里的一个真实 bug：`plugins/memory-compiler/scripts/memory-compiler/session-pack-lifecycle.mjs` 之前接错到了 handoff 路径，导致 lifecycle 刷新不能稳定走到 pack 核心入口；现已改为直接调用 `applySessionPackLifecycleEntry`，本地 `refresh` 实测可正常产出 active pack。
- 16:1x～16:2x：这轮复验里，`review-triage` 已是 **0 open items**，`verify` 仍给出 **trusted / trusted-with-acceptance-samples**，`pipeline-run` 全链通过（facts / threads / continuity / sourceDiscipline / sourceBacklinks / control-plane 都能正常落盘）。可留的判断是：主链路没有新的结构性回退。
- 16:1x～16:2x：`burn-in-run` 仍会报 `runtime probe failed` / `precise scene mismatch (missing)`，但现场更像是 runtime probe 缺活跃场景上下文造成的状态型告警，不是这轮修复引入的新代码回归；后续若要把 burn-in 口径收成全绿，重点应放在 probe 场景构造/验收入口，而不是再回头怀疑 lifecycle 修复本身。
- 16:47～16:54：P1 收尾复验已真正闭合：先重跑 `runtime-probe` 修正 task scene，再重跑 `acceptance-smoke` 到 **128/128 通过**；最新 `control-plane-verify` 为 **trustLevel=trusted**，同时保留 `acceptance sample review` 隔离留样（非 live blocker）。这轮可留的稳定口径：P1 可以按“已收完”对外确认。
- 17:04：老板确认 **直接切 P2，还是老规矩**。当前执行口径继续保持：少说多做、不碎报，按整包推进，等整个 P2 做完再统一回执。
- 【heartbeat补记】17:5x：P2 这轮已补上 live 复核，不再只是“说已经收口”。真实结果是：`memory-compiler` 已把 `controlPlaneMode` 收成仅 `plugin-preferred`，workspace fallback 已拔掉；`npm test` 16/16 通过，plugin-only acceptance 通过，最新 `verify` 仍为 `trusted / trusted-with-acceptance-samples`。但 `doctor` 还不是 fully retire-ready：`legacy-scripts` 已进入 `ready (compat-archive-only)`，`legacy-docs` 与 `compat-bridge` 仍是 `defer`，因此当前更准确口径是“P2 的 plugin-first 已做实，物理退休/清仓还没完全收尾”。
- 【heartbeat补记】18:03：老板追问 P3 进度后做了 live 实扫，结果要掰直：截至此刻**还没有新的 P3 落盘证据**，当前可验证状态仍停在 P2 收口后的那批产物（control-plane/verify 最近更新时间约 17:50）。所以更准确口径是：P3 已拍板开工，但尚未形成新的可验执行产物；现状仍为 `doctor=preflight-only`（`legacy-docs` / `compat-bridge` defer）与 `verify=trusted-with-acceptance-samples`（isolated acceptance samples=4）。
- 【heartbeat补记】18:1x：P3 这轮终于不是口头开工，而是在本地直接真执行并收口。`reports/openclaw-memory-compiler/` 与 `plugins/memory-compiler-bridge/` 已被可恢复地归档到 `_trash/20260322-memory-compiler-p3-retirement/`；4 条 acceptance sample review 已清到 0。最新 live 结果：`doctor=ready-for-manual-retirement`、`destructiveReady=true`；`verify=trusted-and-clear`，warning 清零。也就是说，这次不只是 plugin-first 跑通，而是 legacy-docs / compat-bridge / acceptance samples 这几条尾巴都被真正拔掉了。
- 【heartbeat补记】18:12+：老板继续拍板，把 `scripts/memory-compiler` 也做最终退休/归档；现已迁入 `_trash/20260322-memory-compiler-p3-retirement/scripts-memory-compiler`。这意味着三块 legacy surface（scripts / reports legacy docs / compat bridge）都已从工作位移除。最新 live 结果没塌：`doctor` 仍是 `ready-for-manual-retirement`、`destructiveReady=true`，`verify` 仍是 `trusted-and-clear`、warning 清零，`npm test` 依旧 16/16 通过。可以说这轮已经把 workspace-era 外露 legacy 壳子基本剥干净了。
- 【heartbeat补记】18:2x：P1 最后一层“深抽离”终于不是口头收官，而是把最硬的两根线真拔掉了：`run-script.mjs` 不再回落到 workspace `scripts/memory-compiler`，`plugin-only acceptance` 和 command tests 也不再偷偷 seed workspace script stub。现场三板斧全部过：`cli-smoke` 通过、`node --test test/*.test.mjs` 16/16、`plugin-only-acceptance` 14/14。`MIGRATION.md` 的 Phase 2 口径也已改成“Substantially completed”，不再挂着那串 deeper extraction pending。到这一步，P1 算是真关账了，不是文案关账。
- 【heartbeat补记】19:11+：P3 做了 live checkpoint。结果不是三节点都收完，而是典型的“violet 已清，远端未清”：violet 已 `trusted-and-clear` 且 warning 清零；laoda / lebang 还残留 bridge、legacy scripts、legacy reports，`verify` 仍停在 `trusted-with-acceptance-samples`，并带 stale / acceptance sample warning。也就是说，这轮不能报 P3 完成，最多算“本机过线，远端待收”。
- 【heartbeat补记】19:14+：P3 最终收口完成。之前看起来像卡住，其实是执行层两个小钉子：一次 heredoc 截断，一次远端非登录 shell 没把 `node` 带进 PATH；修掉后继续补跑，最终 laoda / lebang 也都清干净了。现在三节点统一状态是：`plugins/memory-compiler-bridge`、`scripts/memory-compiler`、`reports/openclaw-memory-compiler` 都已从远端工作区移走并归档到 `_trash/20260322-p3-legacy-retire/`；三节点 `verify` 全部是 `trusted-and-clear`，warning 清零，gateway 都是 running。P3 到这一步才算真 finished。
- 【heartbeat补记】19:26：P4 正式开工。目标已经从“插件工程可用”切到“别人拿过去也能安装、启用、doctor、migrate、refresh、verify、publish-check、install smoke 全绿”。这轮主刀不再是 runtime/control-plane，而是文档、安装路径、发布元信息、FAQ 和 clean-install 证据链。
- 【heartbeat补记】19:26+：P4 第一包已经不是嘴上说“更像产品”，而是把主入口真改成 operator-first 了。README 不再把人先丢进迁移史，MIGRATION 退回 maintainer 文档，docs/FAQ 补齐，publish-check 开始真卡文档面和版本一致性，`smoke:clean-install` 也单独立起来了。现场整条验证链都过：cli smoke、16/16 test、plugin-only acceptance、publish-check、npm run check:publish、npm run smoke:clean-install。当前最像样的口径是：P4 第一包已经把“装、启、doctor、migrate、refresh、verify、publish-check、clean-install smoke”这条成品面收平了。
- 【heartbeat补记】19:56+：P4 收官完成。最终四连验全绿：npm test 16/16、clean-install smoke 14/14、trusted-install smoke 7/7、publish-check failedCount=0。过程中修了一个 publish-check 语法尾巴和 trusted-install 缺少 verify blockers 的问题（补齐 integrity/source-discipline/runtime-probe-trend 报告并修正脚本无参调用）。现在已经达到“别人拿过去也能像 memory-lancedb-pro 一样装、跑、验”的口径。
- 【heartbeat补记】20:4x：P5 已收完并真实对外发布。`memory-compiler` 已拆成独立公开仓 `2951461586/openclaw-memory-compiler`，首发 tag/release 为 `v0.2.4`。过程中补齐了 package metadata（repository/bugs/homepage）、README 正式 clone URL，并修掉一个只有放到独立公开仓才会暴露的 docs 路径测试假设。最终独立副本验收仍全绿后才发。
- 22:2x：补查 `laoda` 侧 OpenClaw 运行态时，确认了两个真实系统行为：1) 非 login shell（直接 `ssh laoda '...'`）的 PATH 只有 `/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin`，不会加载 `~/.bashrc` 里的 nvm / PNPM_HOME，因此 `openclaw` 会表现成“找不到/像没装”；而 `bash -lc` 后可正常解析到 `/root/.nvm/versions/node/v22.22.0/bin/openclaw`，再转发到 `/root/.local/share/pnpm/openclaw`。2) `laoda` 的 `openclaw-gateway.service` 当前运行态实际是 **v2026.3.13**，且 pnpm global store 同时残留 `openclaw@2026.3.11` 与 `2026.3.13`，旧版更像缓存残留，不是当前生效版本。
- 22:2x：同轮顺手跑 `laoda` 上的 `openclaw status` 时，命中了一个更值得留档的现场故障：CLI 在 status 汇总阶段触发 **Node heap out of memory**（GC 后 `Reached heap limit Allocation failed - JavaScript heap out of memory`），命令以 255 退出。这说明当前 `gateway active` 不代表 `status` 这类重汇总路径一定稳定；后续若要继续收这条线，优先级应从“版本歧义”转向“laoda 上 `openclaw status` 为何 OOM，以及是否需要调大 Node 内存/缩小 status 载荷/定位某插件或数据面膨胀”。
- 22:5x：继续只读追 `laoda` 的 gateway/status 异常后，现场口径又收紧了一层：不只是 `status --deep`，连 `openclaw health --json`、`nodes status --connected`、`gateway status --json` 都会在 20~25s 内超时、且几乎无 stdout/stderr。与此同时，`openclaw-gateway.service` 进程保持 `active/running`，当前 RSS 仅约 40~80Mi、`MemoryPeak≈923Mi`，请求期间并未再次暴涨；但按 `lsof/ss` 观察，它此刻看不到明确的 TCP 18789 监听，也没扫到明显的 openclaw/gateway Unix socket 文件，只留下一把 `/tmp/openclaw-0/gateway.*.lock`。主机整体资源也很紧（2G 内存仅余约 64Mi、swap 已用 5.7G），同时还有多条高 RSS 的 `openclaw` 进程常驻（约 200~320Mi 级别）。当前更像是：A 已修好 CLI 入口，但 B 的核心根因已从“单次 status 太重”收敛为 **laoda 上 OpenClaw 运行面整体卡滞/入口不对外可用 + 主机内存长期吃紧**；下一步应优先查 gateway 实际监听/transport 期望值与这些高占用 openclaw 子进程的角色，而不是先上来就盲调 PATH 或只给 status 加堆。
- 22:59～23:00：按老板选择执行了 **方案 2：受控重启验活**。`systemctl --user restart openclaw-gateway.service` 后，`MainPID` 已从 `3975822` 换到 `3984914`，`ExecMainStartTimestamp=22:59:26`，新进程 `MemoryPeak` 仅约 `103MB`，说明确实完成了重启且不再沿用旧堆；但问题**没有随重启恢复**：重启后依旧看不到明确的 `:18789` TCP 监听或 openclaw/gateway Unix socket 文件，`openclaw gateway status --json` / `health --json` / `nodes status --connected` 仍全部在 20s 超时，`status --deep` 也继续挂住。当前可确认这不是单纯“旧进程卡死”——更像是 **gateway 新进程启动后就进入了 active/running 但不可服务 的假活状态**；下一步应优先转向核对 systemd user unit 的 `ExecStart/启动参数/transport 预期` 与 CLI 连接模型，而不是继续重复重启。
