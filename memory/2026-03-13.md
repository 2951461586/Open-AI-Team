# 2026-03-13

- 完成三节点（violet / laoda / lebang）SSH 基建收口：三台机 `~/.ssh/config` 统一为同一模板，固定保留 `Host laoda` / `Host violet` / `Host lebang`。
- 三台机 `authorized_keys` 已统一收口为三节点互信；laoda 上重复 key 已清理。
- 三节点互通实测通过：violet → laoda / lebang、laoda → violet / lebang、lebang → violet / laoda 全部 OK。
- lebang 已补齐与其余节点一致的 OpenClaw 运行基线：Node v22.22.0、OpenClaw 2026.3.12、`/root/.nvm/versions/node/v22.22.0` 路径可用。
- lebang 的 `openclaw-gateway.service` 已切换到 NVM 路径并探活通过。
- lebang 已同步并启用自动更新：`scripts/auto-updater.sh`、`openclaw-auto-update.service`、`openclaw-auto-update.timer`；下次执行为 Asia/Shanghai 每天 04:00。
- lebang 旧隧道 `lebang-orchestrator-tunnel.service`（19093）已停用，统一改为 `laoda-webhook-tunnel.service`（19092 → laoda:19090）；`napcat-bridge-critic.service` 应继续指向 `ORCH_URL=http://127.0.0.1:19092/webhook/qq`。
- 新增并部署三台机通用同步脚本 `/usr/local/bin/sync-to`，作为标准同步入口；默认同步 `/root/.openclaw/workspace`，支持传子路径与 `--dry-run` / `--delete`。
- 将三节点 SSH / 同步基线写入长期记忆，并提升为高优先级：后续新增节点、重装系统、迁移 workspace 时优先恢复这一套。
- 升级 `sync-to`：新增目标组 `both` / `all`、`--restart`、`--restart-user`、`--check`，用于同步后批量重启和验活。
- 新增 playbook：`memory/playbooks/three-node-sync-baseline.md`，把三节点 SSH / 同步 / restart / check 的标准操作固化成文档。
- 核对 QQ Bot v2 非频道消息能力后确认：官方发送接口支持 `msg_type` 文本(0) / markdown(2) / ark(3) / embed(4) / media(7)，并可携带 `keyboard`；但本机 `qqbot` 插件原始出站实现只实际封装了 text/markdown/media/input_notify，未接入 ark/embed/keyboard。
- 已在 `/root/.openclaw/extensions/qqbot` 完成 richer-message Phase 1 底座改造：`src/types.ts` 新增 `QQOutboundMessageInput/QQKeyboard/QQArk/QQEmbed` 等类型；`src/api.ts` 新增 `sendC2CStructuredMessage`、`sendGroupStructuredMessage` 及对应 proactive 结构化发送；`src/outbound.ts` 新增 `sendStructured(...)`，支持被动回复超限时降级主动消息。
- 已继续打通 qqbot 上层出站入口：`src/channel.ts` 现在支持从 `channelData.qqbot.message` 旁路进入 `sendStructured(...)`，意味着上层 reply payload 已能显式下发 QQ 结构化消息（如 `markdown + keyboard`、`ark`、`embed`），不再只停留在底层 API。
- 此轮改造后，对 `qqbot` 插件运行 `./node_modules/.bin/tsc --noEmit` 已通过；顺手收掉了原先卡全量编译的两个点：`normalizeTarget` 类型兼容问题，以及 `sendText` 上下文读取 `channelData` 的兼容处理。
