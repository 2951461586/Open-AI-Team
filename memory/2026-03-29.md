# 2026-03-29

- lebang 侧 QQBot 排障：不是整条链路失活，而是 **不同 Bot/AppID 下用户 openid 不同** 导致 `allowFrom` 绑错。source-first 日志确认：lebang 实际收到的 C2C senderId 为 `9E1E97371B7377864260203DF05C67A3`，而配置里只放了 violet 侧 sender `FD54D50FDB86B669EC3195C21BFA0FBB`。
- 2026-03-29 07:55 已在 lebang `/root/.openclaw/openclaw.json` 把 `channels.qqbot.accounts.lebang.allowFrom` 收口为同时包含上述两个 ID，并重启 `openclaw-gateway.service`。备份文件：`/root/.openclaw/openclaw.json.bak.1774742119`。
- 本轮结论：QQBot 排障先看 source-first 网关日志，不要把“命令无响应”直接等同于“收发都挂”；先分清 **消息接收/typing notify 成功** 与 **控制命令因 allowFrom 不匹配被静默挡掉**。
- lebang 近一小时 5 次 `No response within timeout` 的模式已核实：统一呈现 `input_notify`（T+0）→ 第二次 `input_notify`（T+50s）→ `No response within timeout`（T+70s 左右）。主因更像 **长分析/长汇报回复超过约 70 秒硬超时**，不是 memory-lancedb-pro 继续报错；同窗内 `stringEnum` / `auto-recall failed` / embedding 401 均为 0。
- lebang 当前 `cliproxyapi/gpt-5.4` 的 200k 模型注册是正常的，但默认模型与会话元数据还没切干净：`openclaw.json` 里 `agents.defaults.model.primary` 仍是 `baiduqianfancodingplan/qianfan-code-latest`；QQ 直聊 session `agent:lebang:qqbot:direct:9e1e97371b7377864260203df05c67a3` 持久化为 `model=qianfan-code-latest`，同时 `authProfileOverride=cliproxyapi:default`，形成 **session.model 与 auth profile 分裂**。
- status/runtime 源码已确认：`resolveSessionModelRef()` 会优先吃 session entry 里的 `model`，因此 lebang 侧上下文窗口继续显示 qianfan 的 98304（≈98k），不会自动切到 `cliproxyapi/gpt-5.4` 的 200k。若要真正切窗，需要同时处理 **默认模型 primary** 与 **旧 session 重建/清理**。
- 2026-03-29 晚间继续核对 mulerun/CPA 主线时，source-first 配置锚点已确认 **violet 与 lebang 当前都指向 laoda `119.91.63.250:8317/v1`**，且 `mulerun` 侧都已显式出现 `api = anthropic-messages` + `model = claude-opus-4-6` 的新口径，不再是此前记录里的 `violet-opus/openai-completions` 形态。
- laoda 侧 `cliproxyapi/config.yaml` 同步可见 `base-url: http://100.110.244.30:18000/v1`、`name: mulerun/claude-opus-4-6`、`alias: claude-opus-4-6`，以及 `base-url: http://100.110.244.30:18000`、`alias: mule-computer-default`；因此本轮更准确判断是：**三端配置面对齐基本完成，剩余风险点集中在 live auth / client key 对齐，而不是模型映射或路由没收口。**
- 本轮用猜测的旧 client key 直打 laoda `8317 /v1/messages` 返回 `401 Invalid API key`，所以后续若继续验活，必须 **从 laoda 当前 config/secret store source-first 提取现行 client key（不回显）**，再对照 violet / lebang 的 SecretRef；不要再凭历史 key 或摘要口径下结论。
