# 2026-03-09 每日记录

## 上午：Cron 推送问题排查与修复

### 问题
- 早上 8:00 `morning-evolution-surprise` 消息末尾有 `⚠️ ✉️ Message failed`
- 9:00 `github-trending-daily` 消息未送达

### 诊断
1. 对比 laoda 配置发现 `delivery.to` 格式差异
2. 检查日志发现消息实际发送成功，但系统报告失败
3. 发现 cron 子会话 `announce` 绑定为 `null`

### 修复
- 添加 `agentId: "violet"` 到 `github-trending-daily`
- 修正 `delivery.to` 格式：`qqbot:c2c:xxx` → `c2c:xxx`
- 重启 Gateway 清理卡住的运行实例

### 发现的 OpenClaw Bug（已提交 GitHub Issue #40424）
1. **自定义 Provider Usage Tokens 显示 0**：`prompt_tokens`/`completion_tokens` 未映射到 `input`/`output`
2. **Cron Announce Delivery 不工作**：子会话 `announce` 绑定为 `null`，消息需通过 `subagent_announce` + 主会话转发

### 记忆文件更新
- `memory/playbooks/lessons.md` - 添加 cron 配置教训
- `memory/systems/openclaw-config.md` - 添加 cron 配置规范
- `MEMORY.md` - 添加 `delivery.to` 格式要点

## 关键结论

- Cron `delivery.to` 格式：`c2c:xxx` 或 `group:xxx`，**不要**加 `qqbot:` 前缀
- 当前 cron 消息通过 `subagent_announce` 事件 + 主会话转发送达（OpenClaw bug 的变通方案）
