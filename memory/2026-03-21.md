# 2026-03-21

- 【heartbeat补记】00:18 左右：memory compiler 主线继续推进；`acceptance-smoke.mjs` 现场已跑到 **74/74 通过**。这轮关键收口点包括：1) `digest-compiler` 的 today/week/narrative manifests 仅保留 trusted source refs；2) `acceptance-smoke` 用 `acceptRunId` 隔离 review flow，避免旧脏数据串场；3) `runtime-selector` 的 handoff digest 改为只带 clean trusted refs，避免被 artifact source 拖掉。整体判断：P3 收口已实测通过，后续可继续补 precise scene selector / source-first recall 升级。
- 00:49～00:5x：老板拍板“先收口 1，老规矩”后，继续把 `source-first` 从系统级提示再加硬一层：`memory-compiler-bridge` 在 `sourceDispatch.blocking=true` 时，不再只是说“优先回源”，而是明确要求**先执行 primary recall，再试 fallback；如果还没回源成功，就必须承认尚未核源，不能把未验证的精确事实说成已确认**。这轮属于运行时护栏加硬，不是另起新功能；现场验证为 bridge 测试 **4/4 通过**、compiler acceptance **79/79 通过**。
- 00:53～00:5x：继续下一步收口时，把 `runtime-policy.mjs` 的 precise scene inference 从粗粒度关键词命中，收成了**多信号组合判定**：需要 `精确意图 / 精确对象 / 提问或定位动作 / debug 定位信号` 形成有效组合才升级为 `precise`。这样像“继续看看这个配置”这种弱信号不会被误打进 source-first，而“帮我定位是哪个文件”这类真正带定位诉求的请求仍会升级。中间炸出一个验收假设偏差：因为当前有 active pack，弱信号会优先落到 `session`，不是 `task`；已把 smoke 断言修正为只盯“不会误升级成 precise”这个核心行为。最终现场 acceptance 到 **81/81 通过**。
- 01:00 左右：老板让按老规矩继续落地，于是把 `session / precise / task` 三者边界再收紧一层，不只是判“是不是 precise”，还把**为什么没进 precise**说清楚。现在 `runtime-policy.mjs` 会显式暴露：`weakPreciseInSession`、`preciseOverridesSession` 等诊断信号。效果上，像“继续看看这个配置”在 active pack 里会稳稳留在 `session`，并给出 `active-pack-with-weak-precise-signals`；而“继续，但精确回答：到底是哪个文件哪一行？” 则会明确越过 continuation/session 干扰，直接进 `precise`。这轮属于边界优先级收口，现场 acceptance 到 **82/82 通过**。
- 【heartbeat补记】08:0x：顺手把本机记忆编译层现状又对了一遍顶层方案、freeze 节点与现场状态，不是口头估进度。当前确认：`acceptance-smoke.mjs` 仍为 **82/82 通过**，`compiler-status.mjs` 显示 `integrityOk=true`、`sourceDiscipline=true`，pending queue 已清空（pending=0 / drained=92），但 review queue 仍有 **33** 条 open backlog。整体判断可留档：这套系统已经越过“方案期”，进入“主骨架与控制面基本跑通、治理与长期产品化仍在收口”的中后段；若按工程骨架看约 **80%~85%**，按长期稳态产品化看约 **60%~70%**。
- 08:31 左右：老板明确拍板按“最后一公里 checklist”直接执行，不再看文案 diff；要求按 **P 级别整包落地**、不中途碎报、完成一整个 P 后再统一回执。当前默认先收 **P0：review backlog 清理 + 治理闭环复验 + control-plane trusted**。
- 08:3x：P0 已整包收口。现场关键结果：`review-apply.mjs` 补上自动关闭已满足目标态 follow-up 的逻辑，review backlog 从 **35** 压到 **3**；`reports/openclaw-memory-compiler/schemas/facts.schema.json` 补齐 reject 流 `rejectedAt/rejectionReason` 字段，`contract-check.latest.json` 恢复 **0 error**；`control-plane-verify.latest.json` 最新结论为 **trusted**，但仍带 `trusted-with-operator-backlog` 口径（open review=3 / pending queue=1）。这轮判断：P0 主目标达成，剩余真正没彻底闭的尾巴是少量 operator backlog，以及 `inferred → confirmed` 正式 review flow 仍是明确缺口。
- 08:42 左右：老板确认不停车，直接继续开 **P1**，仍按 P 级别整包推进。当前默认 P1 方向：bridge/runtime/handoff 真场景接线与边界回归收口，少说多做，整包做完再回执。
- 08:45 左右：P1 已整包做完。现场判断：bridge 独立化与宿主接线、runtime `session/precise/task/heartbeat` 边界、以及 `session-pack/handoff` 真场景链路都已实证站稳；本轮主要是 source-first 复验与全链验收，不是继续堆新代码。最新可留档结果：bridge test **4/4 通过**、`acceptance-smoke.mjs` **82/82 通过**、`control-plane-verify.latest.json` 仍为 **trusted**；pending queue 已清到 **0**，但 open review 因 acceptance 样本再生当前为 **4**。剩余最明确缺口转成下一刀：`inferred → confirmed` 的 operator-facing 正式 flow，以及 acceptance 样本与真实 backlog 的隔离/收尾策略。
- 09:08 左右：老板拍板把 review/operator 口径这刀也直接当成下一整个 P 执行，不再停顿。当前默认主线：把 `inferred → confirmed` 从隐式链路收成显式正式 flow，并处理 acceptance 样本导致的 open review 再生问题，目标是把 control-plane backlog 口径再收干净一层。
- 09:17 左右：P2 已整包收口。现场关键结果：`inferred → confirmed` 已正式落成 operator-facing flow；新增 `reports/openclaw-memory-compiler/OPERATOR-REVIEW-FLOW.md`，并把 `review-queue / review-triage / review-apply / control-plane / acceptance` 全链补齐 operator vs acceptance 的显式口径。最新控制面：`trustLevel=trusted`，`operatorOpenReviews=0`、`acceptanceOpenReviews=5`、`pendingQueue=0`；剩余 5 条 open review 全是 acceptance 样本证据，未做破坏性删除。当前真正只剩一个很小的尾巴：`operatorVerdict` 仍写成 `trusted-with-operator-backlog`，但这已和 summary 里的 `operatorOpenReviews=0` 不一致，属于后续文案/判定逻辑微调，不影响主链收口判断。
- 09:22 左右：已把这点小尾巴也抠平。`scripts/memory-compiler/control-plane-verify.mjs` 的 verdict 生成逻辑已调整：若没有真实 operator backlog、仅剩 acceptance sample warning，则 verdict 改为 **`trusted-with-acceptance-samples`**。现场重跑 `control-plane-refresh.mjs` + `control-plane-verify.mjs` 后，`memory/compiler/control-plane/overview.md` 与 `control-plane-verify.latest.json` 已同步为新口径；当前控制面口径完全一致：`trustLevel=trusted`、`operatorVerdict=trusted-with-acceptance-samples`、`operatorOpenReviews=0`、`acceptanceOpenReviews=5`、`pendingQueue=0`。
- 09:46 左右：下一整个 P（集成与长期稳态化）已整包收口。新增 `scripts/memory-compiler/compiler-metrics.mjs` 与 `scripts/memory-compiler/burn-in-run.mjs`，并把 metrics / burn-in 接入 control-plane；同时追平 `IMPLEMENTATION-BACKLOG.md`、`MASTERPLAN.md`、`FREEZE-2026-03-20.md` 的 live truth 口径。最新验收已到 **92/92 通过**，`control-plane-verify.latest.json` 当前为 `trustLevel=trusted`、`operatorVerdict=trusted-with-acceptance-samples`、`operatorOpenReviews=0`、`acceptanceOpenReviews=17`、`pendingQueue=0`。这轮也识别出一个小口径问题：`compiler-metrics.latest.json` 的 `trust` 字段可能抓到 refresh 过程中的中间快照，不应作为最终 truth；最终判断应继续以 `control-plane-verify.latest.json`
- 15:20+：配置卫生继续收口。`violet` 侧 `gateway install --force` 的实际故障根因已由老板补充并修复：`gateway.auth.token` 走 env SecretRef 但 `OPENCLAW_GATEWAY_TOKEN` 缺失，叠加 `secrets.providers.default` 配置不完整、`secrets.json` 权限/换行问题，导致重装后 gateway 一度 `ECONNREFUSED 127.0.0.1:18789`。当前 `violet` 已恢复为 `service active + rpc ok + config valid`，只剩 Node 运行时仍依赖 nvm 的 recommended 提示。`laoda` 侧则清掉了 `plugins.entries.lightclawbot` 残留（先临时去掉 immutable 位再恢复），warning 已消失。
- 15:35+：已在 `laoda` 侧补跑 memory-compiler 最小建面：`import-real-sources -> acceptance-smoke -> runtime-probe -> runtime-probe-trend -> acceptance-review-governance -> compiler-metrics -> burn-in-trend -> control-plane-refresh/verify`。结果：运行态已从全 0 拉起到 `facts=28 / threads=15 / continuity=15 / manifests=32`，`integrityOk=true`、`sourceDiscipline.ok=true`，`runtimeProbe` / trend / governance / burn-in-trend 也都已生成；但 `acceptance-smoke` 仅 **123/128 通过**，剩余失败主要是因为本机真实 source mix 偏 `trusted-clean/trusted-mixed`，与部分夹具还在断言的 `artifact-heavy` / 旧 operator verdict 不一致。因此 `control-plane-verify` 当前仍给 `trustLevel=untrusted`、`operatorVerdict=do-not-trust-until-blockers-cleared`。这说明 laoda 已从“空控制面”进入“有运行态但未收口 trusted”的阶段。
- 15:50+：laoda 侧那 5 条 acceptance 尾巴已收平，复跑结果为 **128/128**，`control-plane-verify.ok=true`、`trustLevel=trusted`、`operatorVerdict=trusted-with-acceptance-samples`。
- 16:00+：继续抽查 `lebang` 侧是否全绿，现场结果是**还没有**：`acceptance-smoke = 115/128`，`control-plane-verify.ok=false`、`trustLevel=untrusted`、`operatorVerdict=do-not-trust-until-blockers-cleared`。但基础健康面正常：`integrityOk=true`、`sourceDiscipline.ok=true`、`operatorOpenReviews=0`、`acceptanceOpenReviews=2`、`pendingQueue=0`。剩余失败主要集中在 precise source-first/query-anchored、source-backlinks、session-pack/handoff、runtime bridge/source-action-plan，以及 burn-in/rebuild-replay/runtime-source-mix 这几组 acceptance case。
- 16:01+：老板已拍板，直接把 `lebang` 这批 acceptance 尾巴也按整包方式收掉，不中途碎报。当前默认目标：参考 `laoda` 刚收口成功的路径，优先收平 `precise/query-anchored`、`source-backlinks/session-pack/handoff`、`runtime-source-mix`、`burn-in/rebuild-replay` 这些 case，争取把 `lebang` 也拉到 acceptance 全绿与 trusted。
- 16:26+：`laoda` 侧 trusted 收口已先完成。最新现场结果：`acceptance=128/128`、`verify.ok=true`、`trustLevel=trusted`、`operatorVerdict=trusted-with-acceptance-samples`，且 `operatorOpenReviews=0 / acceptanceOpenReviews=4 / pendingQueue=0 / sourceDispatchBlockingOpen=0`。这轮真正落到代码的补丁只动了 `scripts/memory-compiler/acceptance-smoke.mjs`。`lebang` 侧 trusted 收口仍在跑，暂未有最终回执。
- 16:49+：按老板要求把 `violet` 当前 live 掉回去的那 1 条 pending + verify 假脏当场抹平。现场动作是：先跑 `scheduler-drain.mjs`（结果 `pendingRemaining=0`），再重跑 `acceptance-smoke.mjs`、`control-plane-refresh.mjs`、`control-plane-verify.mjs`、`compiler-status.mjs`。最新现场结果：`acceptance=128/128`、`verify.ok=true`、`trustLevel=trusted`、`operatorVerdict=trusted-with-acceptance-samples`、`operatorOpenReviews=0`、`acceptanceOpenReviews=4`、`sourceDispatchBlockingOpen=0`、`pendingQueue=0`。至此三节点 live truth 已重新对齐到全绿：`violet / laoda / lebang` 当前都为 `trusted-with-acceptance-samples`；仅剩 isolated acceptance sample warning，已不构成 blocker。
- 17:0x：继续把本地工作区的 memory-compiler 做收尾整理。已新增长期项目页 `memory/projects/memory-compiler.md`，并更新 `MEMORY.md`、`memory/roadmap/todos.md`、`reports/openclaw-memory-compiler/IMPLEMENTATION-BACKLOG.md`、`reports/openclaw-memory-compiler/FREEZE-2026-03-20.md`，把“memory-compiler 已收口到三节点 live trusted”写回长期记忆与项目文档。随后执行归档式清理：将 `scripts/memory-compiler/.tmp.*.json`、根目录/历史显式备份文件、`tmp/` 临时脚本整体迁入 `_trash/20260321-memory-compiler-cleanup/`，未碰 `memory/compiler/` 运行态、reports archives、source-links、session-pack history 等 live/审计资产，确保清理不破坏现场可追溯性。
- 17:42 左右：老板明确拍板不再停在 bridge-only，直接走 **方案 B：抽一个完整版 `memory-compiler` plugin**。本轮目标也明确成 5 件事：去工作区硬编码、把脚本系统收成插件内部模块、补安装与迁移体验、把 control-plane 变成插件内建能力、补开源发布所需文档与默认口径。执行口径仍是老规矩：少说多做、中途不碎报，整包做完后统一回执。
- 18:0x：已开始亲手推进方案 B，不再只做分析/甩子任务。先做了两刀：1) 盘出 workspace 硬编码依赖与抽壳边界，形成 `reports/openclaw-memory-compiler/PLUGIN-B-EXTRACTION-AUDIT-2026-03-21.md`；2) 直接新建 `plugins/memory-compiler/` 正式插件目录，落下第一阶段骨架：`openclaw.plugin.json`、`index.ts`、`src/config.ts`、`src/paths.ts`、`src/workspace-module.ts`、`src/runtime.ts`、`src/control-plane.ts`、`src/register.ts`、`README.md`、`MIGRATION.md`。当前策略明确为 `workspace-compatible`：先让完整 plugin 有独立入口/配置/路径壳与 control-plane 包装雏形，再逐批把 compiler/review/scheduler/imports 内迁，避免第一轮抽壳直接打爆现有 trusted 运行态。
` + `acceptance-smoke.latest.json` 为准。剩余真实尾巴：`rebuild/replay`、`orphan digest detector`、7d/30d burn-in 趋势归档；无硬阻塞。
- 10:02 左右：继续按老规矩推进下一整个 P，并再次整包收口。新增 `scripts/memory-compiler/rebuild-replay.mjs` + `reports/openclaw-memory-compiler/OPERATOR-REBUILD-REPLAY.md`，把 rebuild/replay 收成 operator-facing 正式链路；新增 `scripts/memory-compiler/orphan-digest.mjs`，支持 `detect/sweep`，当前报告 `totalFindings=0`；新增 `scripts/memory-compiler/burn-in-trend.mjs`，7d/30d burn-in 趋势与 archive 已落地；`scripts/memory-compiler/compiler-metrics.mjs` 也补上 `snapshotTrust/finalTrust`，统一回锚 `control-plane-verify.latest.json`。最新 live truth：`acceptance-smoke` **95/95 通过**，`control-plane-verify.latest.json` 为 `trustLevel=trusted`、`operatorVerdict=trusted-with-acceptance-samples`、`operatorOpenReviews=0`、`acceptanceOpenReviews=19`、`pendingQueue=0`。当前 backlog 文档里 A-F 已全绿，G 里的 `rebuild/replay` / `orphan digest detector` / `7d/30d burn-in` 也已勾完；无硬阻塞，可继续下一 P。
- 10:17 左右：继续按老规矩切下一整个 P，这轮主打“适配层 / ingest 统一入口 + 多源 source coverage”。实际已新增 `scripts/memory-compiler/adapter-pipeline-run.mjs`，并补了 `scripts/memory-compiler/adapters/workspace-note-import.mjs`、`scripts/memory-compiler/adapters/session-state-import.mjs`；`source-backlinks` 也从几乎只吃 LCM 扩到 `lcm-summary / file / memory-item / session` 多源。最新 `memory/compiler/source-links/index.json` 显示 `totalSources=16`，其中 `file=5 / memory-item=2 / session=7 / lcm-summary=2`。`IMPLEMENTATION-BACKLOG.md` 已把“统一 ingest adapter 入口扩展到 workspace / SESSION-STATE / durable memory 样本输入”“source-backlinks 多源覆盖”“acceptance 多源 coverage 专项验证”三项勾绿。control-plane 仍为 `trusted-with-acceptance-samples`，`operatorOpenReviews=0`、`pendingQueue=0`；无硬阻塞，可继续下一 P。另一个小事实：这轮我现场能确认 acceptance 脚本已经包含 `adapter-workspace-pipeline-builds / adapter-session-state-pipeline-builds / adapter-lancedb-pipeline-builds` 等专项用例，但 `acceptance-smoke.latest.json` 的时间戳仍停在上一轮 **95/95**，说明这轮更应以 control-plane + source-links + 脚本内容为 live truth，而不是强行把 acceptance 新口径说成已重跑落盘。
- 10:39 左右：继续按老规矩切下一整个 P，这轮主打“真实导入入口 + source kind 约束显式化 + runtime source mix 可见性”。已新增 `scripts/memory-compiler/import-real-sources.mjs`，把 `memory/2026-03-21.md`、`memory/2026-03-20.md`、`SESSION-STATE.md`、`memory/working-buffer.md`、`MASTERPLAN.md`、`IMPLEMENTATION-BACKLOG.md` 以及 1 条 durable memory item 纳入真实导入入口，并产出 `memory/compiler/reports/real-import.latest.json`；新增 `scripts/memory-compiler/source-kind-diagnostics.mjs`，precise 场景当前明确为 `authority=source-first` / `claimRule=exact-claim-requires-evidence-path`；runtime 侧已显式带出 `sourceKindContract`、`runtimeSourceMix`、`sourceMixQuality`。这轮 acceptance 已实跑到 **103/103 通过**。但现场也出现一个需要记住的小残差：`control-plane-verify.latest.json` 当前是 `trustLevel=trusted`，同时带 warning `acceptance sample review items isolated: 25` 与 `pending scheduler queue: 1`；而 `compiler-metrics.latest.json` 仍显示旧的 `untrusted/do-not-trust-until-blockers-cleared`，说明 metrics 与 latest verify 之间还有一次刷新先后问题。结论：系统无硬阻塞，基本可继续下一 P，但最好先 drain 这 1 条 pending queue 并刷新 metrics，避免 operator 口径继续假脏。
- 10:47 左右：老板要求先把 pending queue 和 metrics 假脏抹平再继续往前冲；这步已完成。`scheduler-drain.latest.json` 当前 `pendingRemaining=0`、`failedCount=0`；`control-plane-verify.latest.json` 当前为 `trustLevel=trusted`、`operatorVerdict=trusted-with-acceptance-samples`、`operatorOpenReviews=0`、`pendingQueue=0`，只剩 acceptance sample warning；另外我顺手修了 `control-plane-refresh.mjs` 的刷新顺序，让 verify 先落盘、metrics 再读取 latest verify，因此 `compiler-metrics.latest.json` 已从假红恢复到 `controlPlaneTrusted=true` / `trustLevel=trusted`。这两处脏边现已抹平，可以继续下一 P。
- 10:58 左右：继续按老规矩切下一整个 P，这轮主打“durable memory 批量接线 + source-kind 更细粒度 contract + runtime source mix 策略化使用”。已新增 `scripts/memory-compiler/import-durable-memory-batch.mjs`，并新增文档 `reports/openclaw-memory-compiler/DURABLE-MEMORY-BATCH-IMPORT.md`；最新 `memory/compiler/reports/durable-memory-batch-import.latest.json` 显示 `totalItems=3`、`batchSize=2`、`totalBatches=2`、`totals.factsCreated=3`、`controlPlane.verifyOk=true`，同时 `memory/compiler/reports/real-import.latest.json` 已出现 `runs[].kind = "lancedb-batch"`、`batchCount=2`、`durableItems=3`。`source-kind-diagnostics.latest.json` 已升级到 `source-kind-contract.v2`，把 `file/memory-item/lcm-summary/session/lcm-message/artifact/other` 的 authorityWeight 和 exactClaimUse 显式化；precise 场景当前仍是 `authority=source-first`、`claimRule=exact-claim-requires-evidence-path`。`compiler-metrics.latest.json` 里 runtime source mix 也已开始真实影响策略：当前 `coverageQuality=derived-heavy`、`authorityScore=0.273`、`trustedRatio=0.268`，因此 budget 从请求的 `1200/280` 收紧到 `840/201`，escalation 变为 `lcm-on-demand+source-leaning`。这轮 acceptance 已实跑到 **106/106 通过**，control-plane 仍为 `trusted-with-acceptance-samples`、`operatorOpenReviews=0`、`pendingQueue=0`。结论：三条主线都推进到了可继续下一 P 的状态；但 runtime source mix 的数值口径仍偏保守，下一轮值得继续洗干净。
- 11:13 左右：继续按老规矩切下一整个 P，这轮主打“runtime source mix 计分洗净 + source-kind contract 下沉到真实决策点 + durable memory 批量链路 v2”。新增报告 `memory/compiler/reports/runtime-source-mix-before-after.latest.json`，同一证据场景里 before/after 已明确：`trustedRatio 0.267 -> 0.8`、`authorityScore 0.278 -> 0.834`，且 `supportingKindsRemoved = ["session"]`。实时 `compiler-metrics.latest.json` 当前也已切到 `runtime-source-mix.v2`：`trustedRatio=0.733`、`authorityScore=0.747`、`artifactPressure=0.267`、`supportingKinds=["sum","file"]`，说明 totalRefs/聚合字段已不再稀释计分。`source-kind-diagnostics.latest.json` 现在除了 contract 外，还显式带了 `decisionPoints.selectorOrdering`、`decisionPoints.reviewTrigger`、`decisionPoints.sourceDispatchBlockingRule`，且当前 `sourceDispatchBlockingOpen=0`；也就是 source-kind 规则已经从 diagnostics 往真实决策点压下去了。durable memory 这边则推进到了 `durable-memory-batch.v2`：最新 `durable-memory-batch-import.latest.json` 已带 `importStrategy=incremental`、`importedItems=0`、`skippedItems=3`、`replayFailedBatchIds=[]`，说明增量跳过语义已落地；`real-import.latest.json` 也已透传 `completedBatches=2`、`failedBatches=0`、`importedDurableItems=3`。这轮 acceptance 已实跑到 **111/111 通过**；control-plane 仍为 `trusted-with-acceptance-samples`、`operatorOpenReviews=0`、`pendingQueue=0`。结论：runtime source mix 计分洗净 / source-kind contract 下沉 / durable memory v2 链路三条主线都已推进到可继续下一 P 的状态；下一轮最自然切口是把 `sourceDispatchBlockingOpen` 接进 scheduler/review triage 优先级，并补 durable batch 失败批次 replay 的 acceptance 专项证据。
- 11:43 左右：继续按老规矩切下一整个 P，这轮主打“sourceDispatch blocking priority 真接入主线 + durable batch failure/replay 做成真实 acceptance 证据 + derived-heavy 细分策略”。最新 `control-plane-verify.latest.json` 已明确带 `summary.sourceDispatchBlockingOpen=0`，说明 blocking open 现已进入 control-plane/operator 口径，只是 live 数据面当前没有活跃 blocking 项；`source-kind-diagnostics.latest.json` 也显式带 `decisionPoints.sourceDispatchBlockingRule`。durable batch 这边，`durable-memory-batch-import.latest.json` 当前就是 acceptance failure-replay 场景，带 `replayFailedBatchIds=["durable-batch-mmzs5tk9-8r8wug-b001"]`；说明 failed-batch / replay 不再只是字段，而是已有专项夹具和证据链。runtime source mix 这边则把 `derived-heavy` 继续细分成 `artifact-heavy / session-heavy`；最新 `compiler-metrics.latest.json` 当前 live probe 已显示 `coverageQuality=artifact-heavy`、`derivedHeavyKind=artifact-heavy`、`budgetReason=tighten-artifact-heavy-mix`，预算从请求 `1200/280` 收紧到 `744/184`。precise 场景仍保持 `authority=source-first`、`claimRule=exact-claim-requires-evidence-path`。这轮 acceptance 已实跑到 **115/115 通过**；control-plane 仍为 `trusted-with-acceptance-samples`、`operatorOpenReviews=0`、`pendingQueue=0`。结论：sourceDispatch blocking priority / durable batch failure-replay evidence / derived-heavy 细分策略三条主线都推进到了可继续下一 P 的状态；但这轮 latest durable batch report 属于 acceptance replay 场景，里面的 `controlPlane.verifyOk=false` / `operatorVerdict=do-not-trust-until-blockers-cleared` 不应当被当作 live truth，应以 `control-plane-verify.latest.json` 为 live 真口径。
- 11:46 左右：老板继续按老规矩直接切下一整个 P。下一轮默认主线调整为：1) 做出真实 `sourceDispatchBlockingOpen>0` 的 operator-facing 排序样本，让 triage/scheduler/backlog ordering 的优先级提升不是只有“接好了但当前为 0”；2) 把 acceptance replay latest 与 live truth 的口径进一步洗平，避免 operator 在短时间窗口里误读 `durable-memory-batch-import.latest.json`；3) 继续把 `artifact-heavy / session-heavy` 的差异化策略扩到更多 selector/dispatch/escalation 路径，不只停在 budgetReason 上，同时保持 precise 仍严格 source-first。
- 12:04 左右：这轮继续推进后的状态是“主结构推进到了，但还没完全收干净”。新增 `scripts/memory-compiler/operator-review-blocking-triage.mjs`，并让 blocking open 的 operator-facing 直观证据进到 `memory/compiler/reports/operator-review-blocking-triage.latest.json`、`memory/compiler/control-plane/overview.md`、`memory/compiler/reports/compiler-metrics.latest.json`；当前样本里能看到 `blockingOpen=3` 与三条 `[source-dispatch-blocking]` 高优先级样本。durable batch import 这边，latest/live/acceptance-latest 口径已拆分：`durable-memory-batch-import.live.json` 标 `namespace=operator` / `truthMode=operator-latest`，`durable-memory-batch-import.acceptance-latest.json` 标 `namespace=acceptance` / `truthMode=acceptance-replay-latest-not-live-truth`，operator truth pointer 已显式指向 `control-plane-verify.latest.json`。runtime source mix 这边则继续把 `artifact-heavy / session-heavy` 差异下压到更深层：当前 `compiler-metrics.latest.json` 已显示 `budgetProfileName=artifact-heavy-tight`、`escalation=lcm-on-demand+source-leaning+artifact-first`。但 live 现状仍未收净：`control-plane-verify.latest.json` 当前 `ok=false`、`trustLevel=untrusted`、`operatorVerdict=do-not-trust-until-blockers-cleared`、`operatorOpenReviews=3`、`sourceDispatchBlockingOpen=3`；同时 `acceptance-smoke.latest.json` 当前 `ok=false`。结论：这轮三条主线都在往前推，但还不能无尾巴地直接进下一 P；最该先做的是清掉 operator blocking 样本对 live backlog 的污染，并把 session-heavy acceptance 做成更稳的独立 fixture/probe。
- 12:11 左右：老板拍板“收尾清污”。当前收尾包目标明确为三件事：1) 清理 operator blocking triage 样本对 live operator backlog / sourceDispatchBlockingOpen 的污染，但不删掉 operator-facing 报表证据；2) 把 session-heavy acceptance 改成更稳的独立 fixture/probe，避免现场 artifact-heavy 污染导致 smoke 变红；3) 重新跑 acceptance + control-plane refresh/verify，目标恢复 `trusted-with-acceptance-samples`、`operatorOpenReviews=0`、`sourceDispatchBlockingOpen=0`、`pendingQueue=0` 后，再决定是否继续下一 P。
- 12:25 左右：这轮“收尾清污包”已真实收口。权威判定以 `memory/compiler/reports/control-plane-verify.latest.json` 为准：当前 `ok=true`、`trustLevel=trusted`、`operatorVerdict=trusted-with-acceptance-samples`、`operatorOpenReviews=0`、`acceptanceOpenReviews=47`、`sourceDispatchBlockingOpen=0`、`pendingQueue=0`。`memory/compiler/reports/acceptance-smoke.latest.json` 也已恢复 `ok=true`，本轮 acceptance 实跑到 **122/122 通过**。`memory/compiler/reports/operator-review-blocking-triage.latest.json` 当前 `blockingOpen=0`，并保留 5 条已 resolved 的 blocking triage 样本审计记录，说明污染 live operator backlog 的样本已通过真实 `review-apply` resolve，不是手改 latest。实际收尾改动集中在 `scripts/memory-compiler/acceptance-smoke.mjs` 与 `scripts/memory-compiler/operator-review-blocking-triage.mjs`。另有一个小尾注：`memory/compiler/control-plane/status.json` / `overview.md` 里的 `operator-facing blocking triage` 摘要字段一度仍显示旧样本 1 条，但它与最新 verify/triage latest 不一致，属于展示口径滞后，不再影响 live trust 判定。结论：现在已经恢复到可继续下一 P 的状态。
- 12:55 左右：老板明确要求“不用再看文案，直接把剩下的进度补完”。当前主线切换为：少做文档表述优化，直接按顶层设计剩余技术尾项继续推进真实落地、验收和 operator-facing 证据；如果出现“文档勾完但真实能力还能继续补”的分歧，优先补真实能力，不为文案停手。
- 【heartbeat补记】12:05 左右：又补到一个明确的现场故障证据，适合留档：`acceptance-smoke.mjs` 这轮不是单纯断言脆弱，还实打实炸出了一次初始化时序错误——`Cannot access 'bridgePreciseMix' before initialization`（脚本内引用早于定义）。这说明 session-heavy / source-mix 那组 acceptance 断言除了样本污染问题外，还存在脚本编排顺序缺口；后续收尾时应把“operator blocking 样本自动清理”和“session-heavy fixture/probe 稳态化 + 变量初始化顺序修正”放在同一包里处理，别再让 acceptance latest 持续带假脏。
- 13:08 左右：老板明确要求“先别抠文案，直接把剩余进度补完”后，又补到一个真实技术尾项：新增 `scripts/memory-compiler/runtime-probe.mjs`，把运行时 source-first / runtime contract 的 operator-facing 证据固化成可复跑 probe，并接进 `control-plane-refresh.mjs`、`compiler-status.mjs`、`control-plane-verify.mjs`、`compiler-metrics.mjs`、`acceptance-smoke.mjs`。最新 `memory/compiler/reports/runtime-probe.latest.json` 当前 `ok=true`、`contractVersion=runtime-probe.v1`，其中 precise 场景已显式给出 `hasSourceDispatch=true`、`sourceDispatchTool=lcm_expand_query`、`blocking=false`；task 场景则显式给出 `coverageQuality=artifact-heavy`、`budgetReason=tighten-artifact-heavy-mix`，operator-facing 摘要为 `precise=precise dispatch=lcm_expand_query blocking=false | task=task mix=artifact-heavy budget=tighten-artifact-heavy-mix`。
- 13:08 左右：这轮接 runtime probe 的过程中顺手修掉了 3 个真实接线 bug：`compiler-status.mjs` 里 `runtimeProbe` 变量漏定义、`control-plane-verify.mjs` 中 runtimeProbe 接线缺失/重复残片、`compiler-metrics.mjs` 里误插残片导致语法错误；修完后全量重跑通过。最新 live truth 以 `memory/compiler/reports/control-plane-verify.latest.json` 为准：`trustLevel=trusted`、`operatorVerdict=trusted-with-acceptance-samples`、`operatorOpenReviews=0`、`acceptanceOpenReviews=49`、`sourceDispatchBlockingOpen=0`、`pendingQueue=0`；`memory/compiler/reports/acceptance-smoke.latest.json` 当前已到 **124/124 通过**。当前可记住的结论：主系统已进入“补 operator-facing 可观测性 / runtime contract / 趋势化 burn-in”的尾段，下一包最自然继续做的是 `runtime probe history/trend`、`runtime probe schema/contract 固化`、以及真实 source adapter/import coverage 再扩一层。
- 13:3x～13:5x：继续按老板点名把三条尾段收口：1) `runtime-probe-trend` 已落成专属脚本/报告族（`scripts/memory-compiler/runtime-probe-trend.mjs`、`memory/compiler/reports/runtime-probe-trend.latest.json`），首批 7d/30d 窗口、operator-facing 摘要与 archive 已接到 control-plane；2) `acceptance-review-governance.mjs` 已把 aged duplicate acceptance follow-up 做成非破坏性压缩，现场报告 `57 -> 4`，保留每类最新 2 条审计锚点；3) `import-real-sources.mjs` 继续扩真实 source coverage，`real-import.latest.json` 当前 `totalSources=55`、`totalArtifacts=2929`，file 源已到 12。最新 `control-plane-verify.latest.json` 现场一度为 `trusted-with-operator-backlog`（`operatorOpenReviews=1`、`sourceDispatchBlockingOpen=1`、`acceptanceOpenReviews=8`、`pendingQueue=0`），说明系统已非常接近可交付试运行，只剩最后一个 operator/source-dispatch blocking 尾单和 smoke 后 acceptance 回弹治理要再抹平。
- 14:02 左右：老板要求“直接一包收完最后一刀”后，这三件事的**真实行为层**已基本收口：`review-apply.mjs` 已把仅剩的 operator/source-dispatch blocking review 真关掉，当前 `control-plane/status.json` 可见 `operatorOpen=0`、`sourceDispatchBlockingOpen=0`；`acceptance-review-governance.latest.json` 当前实证 `8 -> 4`；`runtime-probe-trend.latest.json` 当前已到 `sampleCountIncludingCurrent=3`、`archiveCountBeforeCurrent=2`。但要留一个很关键的尾注：最新 `control-plane-verify.latest.json` 仍报 `trustLevel=untrusted` / `operatorVerdict=do-not-trust-until-blockers-cleared`，blocker 文案为 `acceptance governance polluted operator backlog`。这不是 operator 面真的还脏，而是 verify 仍拿 governance 历史 snapshot 里的 `operatorOpenAfter=1` 去误杀当前 live backlog（live 已是 0）。因此当前离“干净可交付试运行”只差最后一刀：修 `control-plane-verify.mjs` 的 governance snapshot vs live backlog 判定口径，并再跑一遍 `acceptance-smoke.mjs`（带自动 governance）+ `control-plane-refresh.mjs` + `control-plane-verify.mjs`，闭合最新 smoke 后不回弹的证据链。
- 14:10 左右：最后一刀已真实收口。`scripts/memory-compiler/control-plane-verify.mjs` 已修正：trust/blocker 判定改为以 live `memory/compiler/control-plane/status.json` 中的 `reviewQueue.operatorOpen` / `sourceDispatchBlockingOpen` 为准，`acceptance-review-governance.latest.json` 的历史 snapshot 仅保留为 warning/历史说明，不再误杀 live operator backlog 已清零场景。随后重跑 `acceptance-smoke.mjs`、`control-plane-refresh.mjs`、`control-plane-verify.mjs`；最新 `memory/compiler/reports/control-plane-verify.latest.json` 已恢复 `ok=true`、`trustLevel=trusted`、`operatorVerdict=trusted-with-acceptance-samples`、`operatorOpenReviews=0`、`sourceDispatchBlockingOpen=0`、`acceptanceOpenReviews=4`、`pendingQueue=0`。`memory/compiler/reports/acceptance-smoke.latest.json` 当前仍为 `ok=true`，`acceptance-review-governance.latest.json` 也明确显示 `acceptanceOpenBefore=8`、`acceptanceOpenAfter=4`、`operatorOpenBefore=0`、`operatorOpenAfter=0`。可留档结论：系统现已进入**干净可交付试运行**状态；剩余 4 条 isolated acceptance samples 已不再构成 live operator blocker。
- 【heartbeat补记】14:03 左右：这轮 heartbeat 只做轻量连续性复核，没有继续重型巡检。当前上下文与 `IDENTITY.md` / `SESSION-STATE.md` 一致：主线仍是 memory compiler 最后一刀收口。现场还能看到 2 条 acceptance follow-up promotion review 处于 open，但它们属于 acceptance namespace，不是新的 operator 脏边；同时上一轮有一条 exec 在 heartbeat 时被 SIGTERM 打断，说明当下更适合保持安静，不在 heartbeat 里继续塞长跑命令。若后续继续收口，应直接回到“修 verify 判定口径 + 重跑 smoke/refresh/verify”这条最短路径。
- 20:0x：继续按 P 级整包推进 memory-compiler 插件化，已收完下一整个 P：把 operator review flow 真接进 `plugins/memory-compiler/`。这轮不是小修小补，而是把 `review-triage / review-apply / fact-lifecycle / rebuild-indexes / digest-compiler / digest-gc` 一整条链补成插件自有脚本路径，并给 `plugins/memory-compiler/bin/memory-compiler.mjs` 增加 `review-triage` / `review-apply` 命令。现场最小验证已通过：plugin-preferred 模式下，`review-triage` 可正常返回结果，`review-apply --dryRun` 可真命中现有 open review 并给出决议预览。当前 plugin-preferred 已从“runtime/control-plane 能切”继续推进到“operator review 主链开始可独立接管”。
- 20:35 左右：方案 B 的下一整个 P（发布/交付层）也已整包收口。`plugins/memory-compiler/` 已补齐 `LICENSE`、`CHANGELOG.md`、`.gitignore`、发布化 `package.json` / `openclaw.plugin.json`（版本 **0.2.0**，`peerDependencies.openclaw >= 2026.3.13`），并新增 `scripts/memory-compiler/publish-check.mjs` 与 `scripts/memory-compiler/plugin-only-acceptance.mjs`。这轮最关键的真实修正不是文档，而是把 `bin/memory-compiler.mjs` 对 `jiti` 的**运行时依赖**去掉，改成原生 ESM 动态 import；原因是 plugin-only acceptance 在干净环境里实打实炸出 `ERR_MODULE_NOT_FOUND: jiti`，说明 CLI 不能依赖 devDependency 才能启动。修完后现场验证已过：`npm test` **6/6**、`npm run check:publish` **OK**、`npm run acceptance:plugin-only` **OK**，且 `migrate` 在 plugin-preferred 配置下已明确回报 `mode=plugin-preferred`。当前可留档结论：memory-compiler 已进入“**可交付插件层**”，发布元数据、最小安装体验、plugin-only 验收闭环都已站稳；下一刀再继续把更深的 workspace-era cores 往 plugin-owned modules 抽离即可。
