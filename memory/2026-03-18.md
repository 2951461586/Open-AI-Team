# 2026-03-18

- 【heartbeat补记】00:16：新的一天开始；暂无新增事项。延续“低噪音、连续性优先”的 heartbeat 策略。
- 【heartbeat补记】11:30：今日已确认几条真实系统行为：`clawhub whoami` 当前返回未登录；GitHub Trending 抓取链路今日并非 QQ 截断，源页面 `https://github.com/trending` 实际仅返回 6 个 `article.Box-row`；继续检索 `wechat-access` 后，公开可见最接近的是 `@fengcch/openclaw-wechat-access-plugin` / `reloadggg/wechat-access-plugin`，另一路更成熟活跃的替代方案是 `@agent-wechat/wechat` / `thisnick/agent-wechat`。
- 13:23：将 `proactive-agent` 本地化为 **Proactive Lite**：playbook 已写入 `memory/playbooks/proactive-lite.md`，规则已挂入 `AGENTS.md`，`SESSION-STATE.md` 也已轻重构为更适合记录“当前焦点 / 当前决策 / 当前风险 / 下一步”的 WAL Lite 结构。
- 13:25：已创建 `memory/working-buffer.md` 模板，默认 `INACTIVE`，仅在上下文危险区 / compaction / 长任务时启用；模板包含启用条件、记录规则、禁忌项与条目格式。
- 【heartbeat补记】13:46：本地 `clawhub login` 已启动到浏览器授权阶段（生成本机 `127.0.0.1` 回调链接），但等待授权期间对应 exec 会话被信号 15 中断；当前登录尚未完成，如需继续应重新发起 `clawhub login`。
- 13:48：已验证 `clawhub` 登录补救链路：若浏览器授权后 CLI 回调进程中断，可直接取回调链接中的 token，执行 `clawhub login --token ... --no-browser` 写入本地登录态；随后 `clawhub whoami` 已成功返回账号 `2951461586`。
- 【heartbeat补记】13:53：已确认本地 `clawhub` CLI 版本为 `0.8.0`，npm 当前 latest 也是 `0.8.0`，即本机已是最新；期间用于取 `dist-tags` 的长 JSON 查询进程被 SIGTERM 中断，但用更短的 `npm view clawhub version` 已完成同结论验证。
- 【heartbeat补记】14:17：已按老板要求通过 ClawHub 安装 `openclaw-cli` 与 `openclaw-ops-guardrails` 到本地 `skills/`；其中后者因被标记 `suspicious` 需 `--force`，但装下来的本地内容仅见 `SKILL.md` / references 等文档文件。随后已实际核对多条相关 CLI 帮助输出（含 `status`、`health`、`doctor`、`approvals`、`devices`、`nodes`、`security`、`gateway`、`gateway probe`），确认两技能涉及的关键命令族在当前 OpenClaw 版本中真实存在；结论上 `openclaw-cli` 可作为低风险参考技能，`openclaw-ops-guardrails` 可留作运维 playbook，但使用时仍应按现场 `--help` 复核参数细节。
- 14:30：已按老板要求先做技能收口再补知识边界：收紧了 `skills/openclaw-cli/SKILL.md` 与 `skills/openclaw-ops-guardrails/SKILL.md` 的 frontmatter description 和边界说明，明确它们分别聚焦 **CLI 路由** 与 **运维 playbook**，避免与 `clawddocs` / `node-connect` / `healthcheck` / `qqbot-config` 误触发重叠；同时把这套 OpenClaw 相关技能分工与优先级追加写入 `memory/systems/skills.md`。
- 14:34：已按本机实扫结果重整 `memory/systems/skills.md`：区分了 `workspace/skills` 与 OpenClaw 内置 skill 目录，并补充“目录存在 ≠ 当前会话一定可触发”的说明。另已确认本机 `xray.service` 正在运行（自 2026-03-07 持续 active），本地代理监听正常：SOCKS5 `127.0.0.1:7898`、HTTP `127.0.0.1:7899`；最近日志可见 `clawhub.ai:443` 访问经代理转发成功。
- 15:00：已远程对比 `laoda` 与 `lebang` 的真实 OpenClaw 配置文件（均为 `/root/.openclaw/openclaw.json`，不是 workspace 下的 `openclaw.json`）。确认两边在上下文压缩相关基础配置上基本一致：`contextPruning.mode=cache-ttl`、`compaction.mode=safeguard`、`memoryFlush.enabled=true`、`plugins.slots.contextEngine=lossless-claw`、`plugins.entries.lossless-claw.enabled=true`、`plugins.entries.memory-lancedb-pro.enabled=true`。两边也都存在实际 `lcm.db`（`laoda` 约 270M，`lebang` 约 17M）。当前更像是 `lebang` 虽已启用 LCM / compaction，但真实触发规模明显低于 `laoda`，而不是“少了基础开关导致完全不压缩”。
- 15:16：已在 `lebang` 上完成一次隔离的可控触发测试，使用独立 session-id `lcm-test-20260318` 反复灌入中等长度长消息（单轮约 23k 字符，实际写入 user token 约 5773/轮），确认测试会话真实进入 `lcm.db` 的 `conversations` 表（conversation_id=26）。在累计到 10 轮消息、约 `TOKEN_SUM=52778`（其中 user 约 `51966`）后，`messages/context_items` 持续增长，但 `summaries` 仍保持 24、`lcm.db` 大小仍约 16.9M，未观察到新的 summary 生成。当前结论：`lebang` 的 LCM/会话记录链路是工作的，但这轮负载下尚未触发 summary compaction；因此“无法自动压缩”的体感更像触发阈值/策略未命中，而不是基础配置失效。
- 15:29：补充校正 `lebang` 上下文窗口口径：其静态模型配置并非单一数值，而是至少同时存在 `baiduqianfancodingplan/qianfan-code-latest` 的 `contextWindow=98304` 与 `cliproxyapi/gpt-5.4` 的 `contextWindow=200000`。`openclaw status` 也显示不同 session 正在用不同模型（qqbot direct 为 `gpt-5.4`，main/group 可见 `qianfan-code-latest`）。同时源码确认 `lossless-claw` 并不直接读取 `openclaw.json` 的 model contextWindow 作为阈值，而是依赖运行时传入的 `tokenBudget/currentTokenCount`。后续分析“超过多少 token 仍未压缩”时，必须分清：静态 contextWindow、运行时 tokenBudget、以及 UI/状态卡显示的 token 读数，不可混用。
- 15:46：继续深挖 `lebang` 的 `lcm.db` 后，当前能拿到的最硬结论是：数据库里只有 **1 个** conversation 的消息 token 总量超过 `98k`，即 conversation `3`（`msg_tokens=180916`），而它**已经有 10 个 summaries**，并非“高 token 仍完全不压”。同库中 **没有任何** conversation 的消息 token 总量超过 `200k`；也没有发现那种“`summary_count=0` 且 `messages>32` 且 `fresh tail` 外 raw tokens ≥ `20000`”的会话（结果为 0）。对照组也说明问题：conversation `26`（`lcm-test-20260318`）虽有 `82123` message tokens，但总消息仅 30 条，按默认 `freshTailCount=32` 计算其 `fresh tail` 外 raw tokens 为 0，因此 0 summary 完全符合当前自动 leaf compaction 逻辑。
- 19:56：为核实 `cliproxyapi` 上 `gpt-5.4-mini` 是否真比 `gpt-5.4` 更快更轻，先查了上游 `/models`，确认**确实暴露了 `gpt-5.4-mini`**；但本机当前 `openclaw.json` 只注册了 `gpt-5.4` / `gpt-5.2`，尚未把 `gpt-5.4-mini` 挂进本地模型列表。随后对同一上游直接做小样本非流式基准（相同 prompt、相同 `max_tokens`）：`gpt-5.4` 在 6 次中成功 5 次，成功样本总耗时约 **3.5~5.2s**；`gpt-5.4-mini` 这轮则 **0 次成功**，主要返回 `429 Too Many Requests`，夹 1 次 `503`。当前结论：**无法证明 mini 在该上游当前状态下“更快更轻”**；就可观察结果看，mini 线路当下更像是限流/可用性更差，而不是稳定的轻量快线。
