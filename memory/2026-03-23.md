# 2026-03-23

- 【heartbeat补记】刚跨到新一天，先开档，避免 3 月 22 日后半夜的新状态继续挤在昨天的长日志里。
- 【heartbeat补记】当前连续性仍然很强，但主线已从白天的 memory-compiler 发布/退役收口，切到 `laoda` 侧 gateway/status 异常排查。更准确地说，眼下最值得盯的是：`openclaw-gateway.service` 不再是“秒崩 OOM”，而是被加了 `NODE_OPTIONS=--max-old-space-size=1536` 后进入**高内存假活/无监听**状态。
- 【heartbeat补记】截至此刻可留的真实判断：`laoda` 上 user-level gateway 重启后，crash loop 已暂时止血（进程能活过 40s+，`MemoryCurrent≈1.14GiB`），但 `18789` 仍无监听、`gateway health` 仍无返回，所以问题已从“立刻炸”收敛成“启动阶段卡住或未完成对外服务初始化”。后续如果继续追，不该只盯 OOM，要优先确认**卡在监听前、transport 初始化、还是某段插件/状态汇总链路**。
- 老板开始评估把当前助理/节点迁移到阿里云 ECS 免费试用机（华东1 杭州，2C2G，3 个月），当前处于方案选择阶段；要点不是“能不能装上”，而是 **2GiB 内存是否足够稳、以及应优先选纯净 Ubuntu 22.04 还是其他镜像**。
- 方案已从选型推进到执行准备：老板明确要“迁移 SOP”。当前建议口径是 **Ubuntu 22.04 纯净系统 + 并行迁移/蓝绿切换 + 新机先做轻量稳态验活，不直接硬切**。
- 老板追问是否可用“镜像方式”迁移。当前判断：**可以作为保险/快照回滚手段，但不建议作为本次跨云迁移到 2C2G 新机的主方案**；原因是镜像会把旧机的系统态、缓存、潜在故障和主机特异配置一并带过去，当前更适合“干净系统 + 选择性迁移 OpenClaw/workspace/config”。
- 老板已改走“新机先装最新版 OpenClaw，再由我 SSH 上机完成迁移”的路线，并给出实例密码与待绑定密钥名；起初的真实阻塞点是：提供的 `172.26.58.116` 为内网地址，我这边直探 `22/tcp` 不通，因此不能直接从这里 SSH 上去完成公钥写入。
- 随后老板补充公网 IP `8.136.227.238`。现场已通过密码 SSH 登录并把本机 `~/.ssh/id_ed25519.pub` 写入远端 `/root/.ssh/authorized_keys`，随后再用**密钥登录实测成功**（`SSH_KEY_OK`，hostname=`iZbp1gmuub645lr23izje5Z`，`Linux 5.15.0-170-generic`）。当前新机的 SSH 免密入口已打通，后续可直接上机继续做 OpenClaw 迁移。
- 已在新机创建并启用 `openclaw-gateway.service`（user service），显示为 running（`/usr/bin/node /usr/lib/node_modules/openclaw/dist/index.js gateway --port 18789`）。但随后从本机到该 ECS 的 SSH 连接出现 `banner exchange` 超时，短时间内无法继续远程执行（疑似安全组/防火墙/连接频率限制或实例临时卡顿），需要老板在控制台侧确认 22 入站与 sshd 状态后再继续。
- 2026-03-23 10:0x：按老板明确要求，已 kill 本机 chrome-devtools-mcp / watchdog 相关进程并复验当前为 NONE；后续迁移路线锁定为纯 SSH，不再主动使用 browser/devtools/mcp。随后立刻重试 ECS 8.136.227.238 的 SSH，仍报 "Connection timed out during banner exchange"，当前继续迁移的唯一硬阻塞仍是新机 22 端口/sshd 链路不稳。
- 2026-03-23 10:04：老板确认 sshd 正常，允许继续按纯 SSH 路线推进 ECS 迁移。
- 2026-03-23 11:28：老板确认 sshd 已重启，继续纯 SSH 复连 ECS 并推进迁移验活。
- 2026-03-23 11:29：SSH 已恢复；新机当前不是 22 端口问题，而是 OpenClaw/gateway 未监听 18789。现场首个已见根因：workspace 插件 memory-lancedb-pro 加载失败，报 Cannot find module "openai"。

- 2026-03-23 11:43：已启用 laoda 侧 memory-compiler 插件：
  - 通过安全配置程序：释放文件锁 → 修改 openclaw.json → 恢复文件锁
  - 配置修正：`plugins.entries.memory-compiler.controlPlaneMode` 不是有效key，`memory-compiler-bridge` 不能为 null
  - 验活成功：`openclaw plugins list` 显示 `memory-compiler` **loaded** (版本 0.2.3)
  - 路径：`~/.openclaw/workspace/plugins/memory-compiler/index.ts`
- 2026-03-23 13:36+：老板拍板今天执行新机迁移，提供新阿里云 ECS：公网 `8.136.217.252`、私网 `172.26.58.118`、`Ubuntu 22.04.5 LTS`、`2C2G`。
- 2026-03-23 13:3x：已先用密码 SSH 打通新机，并补入本机 `~/.ssh/id_ed25519.pub` 到远端 `authorized_keys`；随后已验证可直接 **root 免密 SSH** 登录。
- 2026-03-23 13:3x：新机首轮轻量验活结果：系统较干净，磁盘约 `40G`（已用 `2.9G`），当前可用内存约 `1.1GiB`；但 **尚未安装 Node / pnpm / OpenClaw**，`openclaw-gateway.service` 不存在，`18789` 也未监听。判断：当前适合继续走“先补基础运行时与 swap，再装 OpenClaw / 同步 workspace / 验活”的干净迁移路线，而不是直接硬切。
- 2026-03-23 13:4x-13:5x：已在新机完成 **2G swap + Node v22.22.1 + OpenClaw 2026.3.13** 安装；但 `openclaw doctor --non-interactive` 在 2C2G 新机上触发 **JS heap OOM**。
- 2026-03-23 13:5x：按老板要求继续执行 `openclaw setup --workspace ~/.openclaw/workspace --non-interactive` 时，再次触发 **openclaw-setup heap OOM**。随后尝试带 `NODE_OPTIONS=--max-old-space-size=1536` 重跑，但新机很快表现为 **可 ping 通、22 端口能建连、SSH banner 交换超时**，判断为主机/sshd 在低内存压力下进入不稳定状态；当前迁移推进被该远端可用性问题暂时卡住。
- 2026-03-23 15:0x：老板重启 ECS 后，现场确认 SSH 恢复稳定，内存/交换分区状态正常（约 `1.1GiB available`，swap `2GiB` 未用）。由于 `setup` 路线在 2C2G 上不稳，改走**手工最小初始化**并成功落盘：`~/.openclaw/openclaw.json` 仅设置 `agents.defaults.workspace=~/.openclaw/workspace`，并补齐工作区模板文件（`AGENTS.md`、`SOUL.md`、`TOOLS.md`、`USER.md`、`IDENTITY.md`、`HEARTBEAT.md`）。当前下一步可直接推进旧机 `~/.openclaw/` 选择性同步与 gateway/status 验活。
- 2026-03-23 15:33：旧机到 ECS 的整包同步已成功完成：同步对象为 `openclaw.json + workspace + extensions + credentials`，`rsync` 统计为 `24,547` 个常规文件、`471.34MB` 字节，退出码 `0`。但同步完成后立即做远端轻验活时，SSH 再次出现 `Connection timed out during banner exchange`，说明当前阻塞已不在“文件是否到位”，而在 **ECS 上 22 端口/sshd 链路仍有间歇性不稳定**；同步后的 gateway/status 运行态仍待 SSH 恢复后补证。
- 2026-03-23 15:53：老板要求重试 SSH 后，现场确认 SSH 已恢复（`SSH_OK`）。随后串行只读验活拿到关键证据：`openclaw-gateway.service` 当前 `inactive/dead`，`18789` 无监听；执行 `openclaw gateway status --json` 时直接抛出 `openclaw-gateway` **JavaScript heap out of memory**。这说明当前迁移主阻塞已重新收敛为 **gateway/OOM**，不是文件缺失，也不是 SSH 本身持续不可用。
- 2026-03-23 15:56+：继续只读复查后结论修正：gateway daemon **实际上已恢复运行**。证据：`systemctl --user` 显示 `active/running`、`MainPID=2423016`；`ss -ltnp` 显示 `127.0.0.1:18789` / `[::1]:18789` 已监听；`/proc/2423016/status` 与 `systemctl --user show` 显示内存大约 `744~767MB`；本机请求 `http://127.0.0.1:18789/health` 返回 `200 {"ok":true,"status":"live"}`，访问 `/` 也能拿到 OpenClaw Control HTML。当前异常更准确地说是：**`openclaw gateway status --json` 之类 CLI 状态命令在现有配置/插件栈下会触发单独的 Node heap OOM**，不等于 gateway daemon 本身没起来。


