# 2026-03-27

- Mulerun 本机号池代理在 Violet 完成落地收尾：OpenClaw `mulerun` provider 已切到 `http://127.0.0.1:18000`，协议保持 `anthropic-messages`。
- 验收结论：`mulerun/claude-opus-4-6` 已实测可用；`mule-computer-default` 因协议不兼容未验通，已从 OpenClaw 配置与 agent 可选模型中移除，避免误选。
- 架构分工确认：CPA / CLIProxyAPI 继续部署在 laoda 侧；Violet 仅承接本机 Mulerun 代理接入，不再寻找本机 CPA 安装位。
- 经验：对 OpenClaw 配置做“删除数组项/模型摘除”类收尾时，`config.patch` 可能受 merge 语义影响残留旧项；需要时直接准备最终态配置后用 `config.apply` 收口更稳。
- 23:55 前后继续收口三节点路由：laoda / lebang 已统一把 `mulerun` provider 指到 laoda CPA (`http://119.91.63.250:8317/v1`，`api=openai-completions`)，统一暴露 `mulerun/violet-opus`，laoda 旧直连 `mulerun_api_key` 已删除。
- Violet 本机也已切到同一路由：`mulerun` 现在走 laoda CPA，`agents.defaults.model.fallbacks` 已包含 `mulerun/violet-opus`，agent alias 为 `violet-opus`；本地直测 `/models` 可见 `violet-opus`，`/chat/completions` 对 `violet-opus` 返回 200/pong。
- 本轮踩坑：Violet 初次改写时把 `mulerun.apiKey.id` 误写成 `/cliproxyapi_api_key`，触发 `ConfigRuntimeRefreshError: JSON pointer segment \"cliproxyapi_api_key\" does not exist`；正确写法应复用已存在的 SecretRef 路径 `/providers/cliproxyapi/apiKey`，不要凭名字猜 secrets.json 键路径。
- 当前已知尾差：Violet 的 `mulerun.models` 在配置面仍同时可见历史 `claude-opus-4-6` 与新 `violet-opus`；不过统一路由与 `violet-opus` 实际调用已验通。后续若要彻底摘除旧模型尾巴，优先走最终态 `config.apply` 或 source-first 查清该段的归一化/合并语义后再动。
- 2026-03-28 00:09 左右已按最终态 `config.apply` 完成收口：Violet 的 `mulerun.models` 现仅保留 `violet-opus`，历史 `claude-opus-4-6` 尾巴已从配置面清除；重启后再次 source-first 直测 `/models` 与 `/chat/completions(model=violet-opus)` 均通过。
